const SEC_JAVA = {
  id:"java",title:"Core Java",icon:"☕",ib:"rgba(56,189,248,.13)",ic:"#38bdf8",
  grp:"java",bar:"#38bdf8",tglow:"rgba(56,189,248,.07)",
  desc:"OOP, JVM internals, Collections, Streams, Generics, and Java 17/21 features.",
  tags:["OOP","JVM","Streams","Generics","Java 17","Collections"],
  qs:[
    {q:"What is the difference between JDK, JRE, and JVM?",d:"easy",
     a:"<strong>JDK</strong> (Java Development Kit) bundles the compiler (<code>javac</code>), debugger, JavaDoc, and the JRE — everything needed to develop and run Java programs.<br><br><strong>JRE</strong> (Java Runtime Environment) contains the standard class libraries and the JVM — enough to <em>run</em> Java programs but not compile them.<br><br><strong>JVM</strong> (Java Virtual Machine) is the engine that loads bytecode, JIT-compiles hot methods to native machine code, and manages memory (GC). It is platform-specific; the bytecode is platform-neutral.<br><br>Relationship: <code>JDK ⊃ JRE ⊃ JVM</code>."},

    {q:"What are the four pillars of OOP and how does Java implement each?",d:"easy",
     a:"<strong>Encapsulation:</strong> Bundle data (fields) and behaviour (methods) in a class. Mark fields <code>private</code>; expose via public getters/setters. Protects internal state.<br><br><strong>Inheritance:</strong> A subclass acquires fields and methods of its parent via <code>extends</code>. Java supports single-class inheritance. Promotes code reuse.<br><br><strong>Polymorphism:</strong> One interface, many implementations. <em>Compile-time</em> — method overloading (same name, different parameters). <em>Runtime</em> — method overriding + dynamic dispatch (JVM calls the subclass version at runtime).<br><br><strong>Abstraction:</strong> Hide implementation details behind a contract. Achieved with <code>abstract</code> classes (partial implementation) and <code>interface</code>s (pure contract). Callers depend on the abstraction, not the concrete class."},

    {q:"What is the difference between == and .equals() in Java?",d:"easy",
     a:"<code>==</code> compares <strong>references</strong> — it asks \"do these two variables point to the exact same object in memory?\"<br><br><code>.equals()</code> compares <strong>logical content</strong> — defined by the class author to express value equality.<br><br><pre>String a = new String(\"hello\");\nString b = new String(\"hello\");\na == b        // false  (different objects)\na.equals(b)   // true   (same characters)</pre><br>String literals share the String Pool so <code>\"hello\" == \"hello\"</code> is true — but never rely on this. Always use <code>.equals()</code> for strings.<br><br><strong>Contract:</strong> whenever you override <code>equals()</code> you <em>must</em> also override <code>hashCode()</code> so that equal objects produce the same hash — required by <code>HashMap</code> and <code>HashSet</code>."},

    {q:"Explain the Java Memory Model — heap, stack, and metaspace.",d:"medium",
     a:"<strong>Stack</strong> (per-thread): LIFO structure storing primitive local variables, method call frames, and object <em>references</em>. Fast allocation/deallocation via stack pointer. Stack overflow → <code>StackOverflowError</code> (usually deep recursion).<br><br><strong>Heap</strong> (shared): all object instances live here. Divided into:<ul><li><em>Young Gen</em> — Eden + 2 Survivor spaces. Most objects die young (minor GC).</li><li><em>Old Gen</em> — long-lived objects promoted from Young Gen (major/full GC).</li></ul><br><strong>Metaspace</strong> (Java 8+, replaced PermGen): class metadata — bytecode, method signatures, constant pool. Lives in <em>native memory</em>, grows dynamically (no fixed size ⇒ no more <code>OutOfMemoryError: PermGen</code>). Tune with <code>-XX:MaxMetaspaceSize</code>.<br><br><strong>Example showing memory allocation:</strong><br><pre>public class MemoryExample {\n  static int staticVar = 100;        // Metaspace (class metadata)\n  \n  public void createObjects() {\n    int localPrimitive = 10;         // Stack (method frame)\n    String localRef;                 // Stack (reference variable)\n    \n    localRef = new String(\"Hello\"); // Heap (String object)\n                                     // Stack (localRef points to heap)\n    \n    Person p = new Person(\"Alice\");  // Heap (Person object)\n                                     // Stack (p reference)\n    \n    int[] arr = new int[5];          // Heap (array object)\n                                     // Stack (arr reference)\n  }\n}\n\n// Memory layout:\n// ┌─────────────────────────────────────┐\n// │ STACK (Thread-specific)             │\n// ├─────────────────────────────────────┤\n// │ localPrimitive = 10                 │\n// │ localRef = 0x1234 (→ heap)         │\n// │ p = 0x5678 (→ heap)                │\n// │ arr = 0xABCD (→ heap)              │\n// └─────────────────────────────────────┘\n//\n// ┌─────────────────────────────────────┐\n// │ HEAP (Shared)                       │\n// ├─────────────────────────────────────┤\n// │ 0x1234: String{\"Hello\"}            │\n// │ 0x5678: Person{name=\"Alice\"}       │\n// │ 0xABCD: int[5]{0,0,0,0,0}          │\n// └─────────────────────────────────────┘\n//\n// ┌─────────────────────────────────────┐\n// │ METASPACE (Native Memory)           │\n// ├─────────────────────────────────────┤\n// │ Class: MemoryExample                │\n// │   - Methods: createObjects()        │\n// │   - Static fields: staticVar        │\n// │ Class: Person                       │\n// │   - Fields, methods, bytecode       │\n// └─────────────────────────────────────┘</pre><br><strong>Key rules:</strong><ul><li>Primitives in methods → Stack</li><li>Objects (any new keyword) → Heap</li><li>References to objects → Stack (just the pointer)</li><li>Class definitions → Metaspace</li></ul>"},

    {q:"How does HashMap work internally in Java 8+?",d:"hard",
     a:"<code>HashMap</code> uses an <strong>array of buckets</strong> (default 16).<br><br><strong>put(key, value):</strong><ol><li>Compute <code>hash = key.hashCode()</code>; spread bits via internal hash function.</li><li><code>index = hash & (capacity-1)</code> — find bucket.</li><li>If empty, insert <code>Node(key, value, hash, next=null)</code>.</li><li>If collision, compare <code>hash</code> and <code>equals()</code>: match → update; no match → append to chain.</li></ol><br><strong>Java 8 treeification:</strong> when a bucket chain length &gt; 8 <em>and</em> table size ≥ 64, the linked list converts to a Red-Black tree → O(log n) worst-case instead of O(n).<br><br><strong>Resize:</strong> when <code>size &gt; capacity × loadFactor</code> (default 0.75), capacity doubles and all entries are rehashed. Expensive — pre-size with <code>new HashMap&lt;&gt;(expectedSize / 0.75 + 1)</code>."},

    {q:"What is the difference between String, StringBuilder, and StringBuffer?",d:"easy",
     a:"<strong>String:</strong> <em>immutable</em>. Every concatenation (<code>+</code>, <code>concat()</code>) creates a new object. JVM may optimise compile-time constants. The String Pool caches literals.<br><br><strong>StringBuilder:</strong> <em>mutable</em>, not thread-safe. <code>append()</code> is O(1) amortised. Use in single-threaded code — loops, builders.<br><br><strong>StringBuffer:</strong> <em>mutable</em>, <em>thread-safe</em> (all methods are <code>synchronized</code>). Slower than StringBuilder due to locking. Only use when multiple threads genuinely share the buffer (rare).<br><br>For single-threaded string building always use <code>StringBuilder</code>."},

    {q:"What are generics and what is type erasure?",d:"medium",
     a:"Generics provide <strong>compile-time type safety</strong> for collections, methods, and classes. <code>List&lt;String&gt;</code> prevents accidentally adding an <code>Integer</code> — caught at compile time, no cast needed on retrieval.<br><br><strong>Type erasure:</strong> the compiler removes all generic type parameters at compile time, replacing them with their upper bounds (usually <code>Object</code>). At runtime, <code>List&lt;String&gt;</code> and <code>List&lt;Integer&gt;</code> are both just <code>List</code>.<br><br><strong>Consequences of erasure:</strong><ul><li>Cannot do <code>new T()</code> or <code>new T[n]</code></li><li>Cannot do <code>instanceof List&lt;String&gt;</code></li><li>Cannot overload on generic type alone</li></ul><br>Use bounded wildcards: <code>&lt;? extends Number&gt;</code> (covariant read) and <code>&lt;? super Integer&gt;</code> (contravariant write) — PECS principle."},

    {q:"What is the difference between an abstract class and an interface (Java 8+)?",d:"medium",
     a:"<table><tr><td></td><td><strong>Abstract class</strong></td><td><strong>Interface</strong></td></tr><tr><td>Instance fields</td><td>✅ Yes</td><td>❌ No (only constants)</td></tr><tr><td>Constructors</td><td>✅ Yes</td><td>❌ No</td></tr><tr><td>Concrete methods</td><td>✅ Yes</td><td>✅ <code>default</code> / <code>static</code> (Java 8+)</td></tr><tr><td>Multiple inheritance</td><td>❌ One parent only</td><td>✅ Implement many</td></tr><tr><td>Access modifiers</td><td>Any</td><td>Public/package by default</td></tr></table><br><strong>Rule of thumb:</strong> use an <em>interface</em> to define a capability contract (<code>Serializable</code>, <code>Runnable</code>); use an <em>abstract class</em> when subclasses share common state, a constructor, or a partial implementation (Template Method pattern)."},

    {q:"What are functional interfaces and how do lambdas relate to them?",d:"medium",
     a:"A <strong>functional interface</strong> has exactly one abstract method (SAM — Single Abstract Method). Examples: <code>Runnable</code>, <code>Callable&lt;V&gt;</code>, <code>Comparator&lt;T&gt;</code>, <code>Predicate&lt;T&gt;</code>, <code>Function&lt;T,R&gt;</code>, <code>Supplier&lt;T&gt;</code>, <code>Consumer&lt;T&gt;</code>.<br><br><code>@FunctionalInterface</code> is optional but makes the compiler enforce the SAM constraint — good practice.<br><br><strong>Lambdas</strong> are concise anonymous implementations of functional interfaces:<br><pre>// Old\nComparator&lt;String&gt; c = new Comparator&lt;String&gt;() {\n  public int compare(String a, String b) { return a.compareTo(b); }\n};\n// Lambda\nComparator&lt;String&gt; c = (a, b) -&gt; a.compareTo(b);\n// Method reference\nComparator&lt;String&gt; c = String::compareTo;</pre><br><strong>How to invoke lambdas:</strong><br><pre>// Creating lambda\nPredicate&lt;String&gt; isLong = s -&gt; s.length() &gt; 5;\n\n// Invoking lambda\nboolean result = isLong.test(\"hello\");      // false\nboolean result2 = isLong.test(\"hello world\"); // true\n\n// Using directly without variable\nList&lt;String&gt; list = List.of(\"hi\", \"hello\", \"greetings\");\nlist.stream()\n    .filter(s -&gt; s.length() &gt; 5)  // lambda invoked by filter()\n    .forEach(System.out::println);  // \"greetings\"</pre><br><strong>Custom functional interface:</strong><br><pre>@FunctionalInterface\npublic interface StringProcessor {\n  String process(String input);\n  \n  // Can have default methods\n  default String processUpper(String input) {\n    return process(input).toUpperCase();\n  }\n  \n  // Can have static methods\n  static StringProcessor identity() {\n    return s -&gt; s;\n  }\n}\n\n// Usage\nStringProcessor trimmer = String::trim;\nStringProcessor reverser = s -&gt; new StringBuilder(s).reverse().toString();\n\nString result = trimmer.process(\"  hello  \");  // \"hello\"\nString result2 = reverser.process(\"java\");     // \"avaj\"</pre><br><strong>Common functional interfaces with examples:</strong><br><br><strong>Predicate&lt;T&gt;</strong> — boolean test(T t):<br><pre>Predicate&lt;String&gt; isEmpty = String::isEmpty;\nPredicate&lt;String&gt; isBlank = String::isBlank;\nPredicate&lt;String&gt; isEmptyOrBlank = isEmpty.or(isBlank);\nPredicate&lt;String&gt; hasContent = isEmpty.negate();\n\n// Invoke\nhasContent.test(\"hello\");  // true</pre><br><strong>Function&lt;T,R&gt;</strong> — R apply(T t):<br><pre>Function&lt;String, Integer&gt; parse = Integer::parseInt;\nFunction&lt;Integer, String&gt; format = i -&gt; String.format(\"%03d\", i);\nFunction&lt;String, String&gt; pipeline = parse.andThen(format);\n\n// Invoke\nString result = pipeline.apply(\"42\");  // \"042\"</pre><br><strong>Consumer&lt;T&gt;</strong> — void accept(T t):<br><pre>Consumer&lt;String&gt; print = System.out::println;\nConsumer&lt;String&gt; log = s -&gt; logger.info(s);\nConsumer&lt;String&gt; printAndLog = print.andThen(log);\n\n// Invoke\nprintAndLog.accept(\"Hello\");  // prints and logs</pre><br><strong>Supplier&lt;T&gt;</strong> — T get():<br><pre>Supplier&lt;Double&gt; random = Math::random;\nSupplier&lt;LocalDateTime&gt; now = LocalDateTime::now;\n\n// Invoke\ndouble value = random.get();  // generates random number</pre><br><strong>BiFunction&lt;T,U,R&gt;</strong> — R apply(T t, U u) — two arguments:<br><pre>BiFunction&lt;Integer, Integer, Integer&gt; add = (a, b) -&gt; a + b;\nBiFunction&lt;String, Integer, String&gt; repeat = (s, n) -&gt; s.repeat(n);\n\n// Invoke\nint sum = add.apply(5, 3);           // 8\nString result = repeat.apply(\"Hi\", 3); // \"HiHiHi\"</pre><br><strong>BiPredicate&lt;T,U&gt;</strong> — boolean test(T t, U u):<br><pre>BiPredicate&lt;String, Integer&gt; isLongerThan = (s, len) -&gt; s.length() &gt; len;\n\n// Invoke\nboolean result = isLongerThan.test(\"hello\", 3);  // true</pre><br><strong>BiConsumer&lt;T,U&gt;</strong> — void accept(T t, U u):<br><pre>BiConsumer&lt;String, Integer&gt; printWithIndex = (s, i) -&gt; \n  System.out.println(i + \": \" + s);\n\n// Invoke\nprintWithIndex.accept(\"hello\", 0);  // \"0: hello\"</pre><br><strong>UnaryOperator&lt;T&gt;</strong> — T apply(T t) — Function&lt;T,T&gt; specialization:<br><pre>UnaryOperator&lt;Integer&gt; square = x -&gt; x * x;\nUnaryOperator&lt;String&gt; upper = String::toUpperCase;\n\n// Invoke\nint result = square.apply(5);      // 25\nString result2 = upper.apply(\"hi\"); // \"HI\"</pre><br><strong>BinaryOperator&lt;T&gt;</strong> — T apply(T t1, T t2) — BiFunction&lt;T,T,T&gt; specialization:<br><pre>BinaryOperator&lt;Integer&gt; max = Integer::max;\nBinaryOperator&lt;String&gt; concat = (a, b) -&gt; a + b;\n\n// Invoke\nint result = max.apply(10, 20);        // 20\nString result2 = concat.apply(\"Hello\", \" World\");  // \"Hello World\"\n\n// Common in reduce operations\nList&lt;Integer&gt; nums = List.of(1, 2, 3, 4, 5);\nint sum = nums.stream().reduce(0, Integer::sum);  // BinaryOperator</pre>"},

    {q:"Explain Java 8 Streams — lazy evaluation and the intermediate vs terminal distinction.",d:"medium",
     a:"A Stream is a declarative pipeline of operations over a data source (collection, array, I/O, infinite generator).<br><br><strong>Intermediate operations</strong> are <em>lazy</em> — they return a new Stream and nothing executes yet: <code>filter()</code>, <code>map()</code>, <code>flatMap()</code>, <code>distinct()</code>, <code>sorted()</code>, <code>peek()</code>, <code>limit()</code>, <code>skip()</code>.<br><br><strong>Terminal operations</strong> <em>trigger</em> the pipeline: <code>collect()</code>, <code>forEach()</code>, <code>reduce()</code>, <code>count()</code>, <code>findFirst()</code>, <code>anyMatch()</code>, <code>toList()</code> (Java 16).<br><br><strong>Short-circuiting:</strong> <code>findFirst()</code> stops after the first match — without lazy evaluation you'd process every element. This makes infinite streams like <code>Stream.iterate(0, n-&gt;n+1)</code> practical.<br><br><strong>Parallel streams:</strong> <code>list.parallelStream()</code> splits work across ForkJoinPool threads — only beneficial for CPU-heavy, stateless operations on large datasets."},

    {q:"What is Optional and when should you NOT use it?",d:"medium",
     a:"<code>Optional&lt;T&gt;</code> is a container that explicitly models the potential absence of a value — a better alternative to returning <code>null</code>.<br><br><strong>Key API:</strong> <code>of()</code>, <code>ofNullable()</code>, <code>empty()</code>, <code>isPresent()</code>, <code>get()</code>, <code>orElse(default)</code>, <code>orElseGet(supplier)</code>, <code>orElseThrow()</code>, <code>map(fn)</code>, <code>flatMap(fn)</code>, <code>ifPresent(consumer)</code>.<br><br><strong>Optional chaining example:</strong><br><pre>// Nested property access with map()\nString city = Optional.ofNullable(user)\n  .flatMap(User::getAddress)     // Optional&lt;Address&gt;\n  .map(Address::getCity)          // Optional&lt;String&gt;\n  .orElse(\"Unknown\");\n\n// Chain with filter\nString email = order.getCustomer()\n  .map(Customer::getEmail)\n  .filter(e -&gt; e.contains(\"@\"))\n  .map(String::toLowerCase)\n  .orElseThrow(() -&gt; new IllegalStateException(\"Invalid email\"));\n\n// Unwrap nested Optional\nOptional&lt;Optional&lt;String&gt;&gt; nested = Optional.of(Optional.of(\"value\"));\nString result = nested.flatMap(Function.identity()).orElse(\"default\");</pre><br><strong>Do NOT use Optional as:</strong><ul><li>Method parameters — use overloads instead</li><li>Collection elements — use an empty collection</li><li>Serialized fields — Jackson/JSON issues, extra weight</li><li>Performance-critical hot paths — object allocation overhead</li></ul><br>Best used as a <em>return type</em> for methods that may genuinely not produce a value."},

    {q:"How does garbage collection work? Explain G1GC vs ZGC.",d:"hard",
     a:"JVM uses <strong>generational GC</strong> based on the observation that most objects die young.<br><br><strong>G1GC</strong> (default since Java 9): divides the heap into equal-sized regions (~1–32 MB). Tracks \"liveness\" per region and collects the regions with most garbage first (Garbage-First). Targets a configurable pause-time goal (<code>-XX:MaxGCPauseMillis=200</code>). Concurrent marking phase runs alongside the application. Suitable for heaps 4 GB–100 GB.<br><br><strong>ZGC</strong> (production-ready Java 15+): performs almost all work concurrently with the application using load barriers. Achieves sub-millisecond pauses even on multi-terabyte heaps. Does not require a pause-time tuning parameter. Use ZGC for latency-sensitive services on very large heaps.<br><br><strong>Shenandoah</strong> (OpenJDK): similar concurrent approach to ZGC, available in some distributions."},

    {q:"What are Java records and sealed classes (Java 16–17)?",d:"medium",
     a:"<strong>Records</strong> (stable Java 16): immutable data carriers. The compiler auto-generates canonical constructor, accessors (no get prefix), <code>equals()</code>, <code>hashCode()</code>, <code>toString()</code>.<br><pre>record Point(int x, int y) {}\nPoint p = new Point(3, 4);\np.x(); // 3</pre>Custom compact constructor for validation: <code>Point { if (x &lt; 0) throw new IllegalArgumentException(); }</code><br><br><strong>Sealed classes</strong> (stable Java 17): restrict which classes can extend/implement a type.<br><pre>sealed interface Shape permits Circle, Rectangle, Triangle {}</pre>Enables <em>exhaustive pattern matching</em> in switch expressions — compiler knows all subtypes so no default case needed. Together they form a powerful algebraic data type system."},

    {q:"What is the difference between Comparable and Comparator?",d:"medium",
     a:"<code>Comparable&lt;T&gt;</code>: implemented <em>inside</em> the class. Defines the <strong>natural ordering</strong>. <code>int compareTo(T other)</code>. Used by <code>Collections.sort()</code>, <code>TreeSet</code>, <code>TreeMap</code> by default.<br><br><code>Comparator&lt;T&gt;</code>: external comparison strategy. Use when you don't own the class, need a different ordering, or need multiple orderings.<br><br><strong>Comparable example (natural ordering inside the class):</strong><br><pre>public class Employee implements Comparable&lt;Employee&gt; {\n  private String name;\n  private int age;\n  \n  // Constructor, getters...\n  \n  @Override\n  public int compareTo(Employee other) {\n    // Natural ordering by age\n    return Integer.compare(this.age, other.age);\n  }\n}\n\n// Usage - natural ordering\nList&lt;Employee&gt; employees = new ArrayList&lt;&gt;();\nemployees.add(new Employee(\"Alice\", 30));\nemployees.add(new Employee(\"Bob\", 25));\nemployees.add(new Employee(\"Charlie\", 35));\n\nCollections.sort(employees);  // sorts by age (natural ordering)\n// Result: [Bob(25), Alice(30), Charlie(35)]\n\n// Works automatically with TreeSet\nTreeSet&lt;Employee&gt; set = new TreeSet&lt;&gt;();\nset.add(new Employee(\"Alice\", 30));\nset.add(new Employee(\"Bob\", 25));\n// Automatically sorted by age</pre><br><strong>Comparator example (external comparison strategy):</strong><br><pre>// Comparator for name (different ordering)\nComparator&lt;Employee&gt; byName = Comparator.comparing(Employee::getName);\nCollections.sort(employees, byName);\n// Result: [Alice(30), Bob(25), Charlie(35)]\n\n// Multi-field comparator (Java 8+)\nComparator&lt;Employee&gt; byAgeAndName = Comparator\n  .comparing(Employee::getAge)\n  .thenComparing(Employee::getName)\n  .reversed();\n\nemployees.sort(byAgeAndName);\n// Result: [Charlie(35), Alice(30), Bob(25)] (descending age, then name)\n\n// Multiple comparators for same class\nComparator&lt;Employee&gt; byAge = Comparator.comparing(Employee::getAge);\nComparator&lt;Employee&gt; byName = Comparator.comparing(Employee::getName);\nComparator&lt;Employee&gt; byAgeThenName = byAge.thenComparing(byName);\n\n// Null-safe comparator\nComparator&lt;Employee&gt; nullSafe = Comparator\n  .nullsFirst(Comparator.comparing(Employee::getName));\n\n// Using with TreeMap (custom order)\nTreeMap&lt;Employee, String&gt; map = new TreeMap&lt;&gt;(byName);\nmap.put(new Employee(\"Charlie\", 35), \"Engineer\");\nmap.put(new Employee(\"Alice\", 30), \"Manager\");\n// Sorted by name, not age</pre><br><strong>When to use each:</strong><br><ul><li><strong>Comparable:</strong> When there's one obvious natural ordering (e.g., Integer by value, String alphabetically)</li><li><strong>Comparator:</strong> When you need multiple orderings, don't own the class, or want external control</li></ul><br><code>Comparator.naturalOrder()</code> and <code>Comparator.reverseOrder()</code> are utility comparators for natural ordering. <code>Comparator.nullsFirst/nullsLast()</code> safely handles nulls."},

    {q:"What is the difference between HashMap, LinkedHashMap, and TreeMap?",d:"medium",
     a:"<strong>HashMap:</strong> O(1) avg get/put/remove. <em>No ordering guarantee</em>. Best choice for pure key-value lookup.<br><br><strong>LinkedHashMap:</strong> O(1) operations. Maintains <em>insertion order</em> (default) or <em>access order</em> (constructor flag). The access-order variant implements an LRU cache when combined with <code>removeEldestEntry()</code>.<br><br><strong>TreeMap:</strong> O(log n) all operations. Keys kept in <em>sorted order</em> (natural or Comparator). Supports range views: <code>subMap(from, to)</code>, <code>headMap(to)</code>, <code>floorKey(k)</code>, <code>ceilingKey(k)</code>. Backed by a Red-Black tree. Use when you need ordered iteration or range queries.<br><br><strong>HashMap example (no order guarantee):</strong><br><pre>Map&lt;String, Integer&gt; hashMap = new HashMap&lt;&gt;();\nhashMap.put(\"Charlie\", 35);\nhashMap.put(\"Alice\", 30);\nhashMap.put(\"Bob\", 25);\n\nSystem.out.println(hashMap);\n// Output: {Bob=25, Alice=30, Charlie=35} (unpredictable order)\n// May change between runs or after rehashing\n\n// Best for: fast lookups\nint age = hashMap.get(\"Alice\");  // O(1)</pre><br><strong>LinkedHashMap example (insertion order):</strong><br><pre>Map&lt;String, Integer&gt; linkedMap = new LinkedHashMap&lt;&gt;();\nlinkedMap.put(\"Charlie\", 35);\nlinkedMap.put(\"Alice\", 30);\nlinkedMap.put(\"Bob\", 25);\n\nSystem.out.println(linkedMap);\n// Output: {Charlie=35, Alice=30, Bob=25} (insertion order preserved)\n// Always iterates in the order items were added\n\n// Access-order mode (LRU cache)\nMap&lt;String, Integer&gt; lruCache = new LinkedHashMap&lt;&gt;(16, 0.75f, true) {\n  @Override\n  protected boolean removeEldestEntry(Map.Entry eldest) {\n    return size() &gt; 3;  // max 3 entries\n  }\n};\n\nlruCache.put(\"A\", 1);\nlruCache.put(\"B\", 2);\nlruCache.put(\"C\", 3);\nlruCache.get(\"A\");     // access A (moves to end)\nlruCache.put(\"D\", 4);  // evicts B (least recently used)\nSystem.out.println(lruCache);  // {C=3, A=1, D=4}</pre><br><strong>TreeMap example (sorted order):</strong><br><pre>Map&lt;String, Integer&gt; treeMap = new TreeMap&lt;&gt;();\ntreeMap.put(\"Charlie\", 35);\ntreeMap.put(\"Alice\", 30);\ntreeMap.put(\"Bob\", 25);\n\nSystem.out.println(treeMap);\n// Output: {Alice=30, Bob=25, Charlie=35} (alphabetically sorted)\n\n// Range operations\nSortedMap&lt;String, Integer&gt; subMap = treeMap.subMap(\"Alice\", \"Charlie\");\nSystem.out.println(subMap);  // {Alice=30, Bob=25}\n\nString firstKey = treeMap.firstKey();  // \"Alice\"\nString lastKey = treeMap.lastKey();    // \"Charlie\"\n\n// Floor and ceiling\nString floor = treeMap.floorKey(\"Brian\");    // \"Bob\" (largest &lt;= \"Brian\")\nString ceiling = treeMap.ceilingKey(\"Brian\"); // \"Charlie\" (smallest &gt;= \"Brian\")\n\n// Custom comparator (reverse order)\nMap&lt;String, Integer&gt; reverseMap = new TreeMap&lt;&gt;(Comparator.reverseOrder());\nreverseMap.put(\"Alice\", 30);\nreverseMap.put(\"Bob\", 25);\nreverseMap.put(\"Charlie\", 35);\nSystem.out.println(reverseMap);  // {Charlie=35, Bob=25, Alice=30}</pre><br><strong>Performance comparison:</strong><br><table><tr><td><strong>Operation</strong></td><td><strong>HashMap</strong></td><td><strong>LinkedHashMap</strong></td><td><strong>TreeMap</strong></td></tr><tr><td>get/put/remove</td><td>O(1)</td><td>O(1)</td><td>O(log n)</td></tr><tr><td>Iteration order</td><td>Random</td><td>Insertion/Access</td><td>Sorted</td></tr><tr><td>Null keys</td><td>✅ 1 null key</td><td>✅ 1 null key</td><td>❌ No null keys</td></tr><tr><td>Memory</td><td>Low</td><td>Medium (extra links)</td><td>Medium (tree nodes)</td></tr></table><br><strong>When to use each:</strong><br><ul><li><strong>HashMap:</strong> Default choice — fastest, no ordering needed</li><li><strong>LinkedHashMap:</strong> Need predictable iteration order, LRU cache</li><li><strong>TreeMap:</strong> Need sorted keys, range queries, floor/ceiling operations</li></ul>"},

    {q:"What are WeakReference, SoftReference, and PhantomReference?",d:"hard",
     a:"Java has four reference strengths (strongest to weakest):<br><br><strong>Strong (normal):</strong> object never GC'd while reachable.<br><br><strong>SoftReference:</strong> collected only when JVM is under memory pressure. Ideal for memory-sensitive caches — entries survive as long as heap is available.<br><br><strong>WeakReference:</strong> collected at the <em>next</em> GC cycle regardless of heap size. Used in <code>WeakHashMap</code> — entries auto-evict when the key has no other strong reference.<br><br><strong>PhantomReference:</strong> enqueued to a <code>ReferenceQueue</code> <em>after</em> the object is finalized but before memory is reclaimed. Use for post-mortem cleanup (replacing deprecated <code>finalize()</code>). Java 9+ <code>Cleaner</code> is the preferred API.<br><br>All three can be combined with a <code>ReferenceQueue</code> to receive notification when the object is collected."},

    {q:"What is the difference between checked and unchecked exceptions?",d:"medium",
     a:"<strong>Checked exceptions</strong> extend <code>Exception</code> but not <code>RuntimeException</code>. The compiler forces you to either <code>catch</code> them or declare them with <code>throws</code>. Examples: <code>IOException</code>, <code>SQLException</code>, <code>ParseException</code>.<br><br><strong>Unchecked exceptions</strong> extend <code>RuntimeException</code> (or <code>Error</code>). No declaration required. Examples: <code>NullPointerException</code>, <code>IllegalArgumentException</code>, <code>IndexOutOfBoundsException</code>.<br><br><strong>When to use each:</strong> checked for <em>recoverable</em> external conditions (file not found, network error — caller can react meaningfully); unchecked for <em>programming errors</em> or unrecoverable conditions. Modern Java and frameworks (Spring) strongly prefer unchecked to avoid cluttered method signatures and forced catch blocks that swallow exceptions."},

    {q:"What are virtual threads (Java 21) and how do they change backend development?",d:"hard",
     a:"<strong>Virtual threads</strong> (Project Loom, stable Java 21) are JVM-managed, lightweight threads — not mapped 1:1 to OS threads. The JVM scheduler multiplexes millions of virtual threads onto a small pool of OS carrier threads.<br><br><strong>Key behaviour:</strong> when a virtual thread blocks on I/O (JDBC, HTTP, file read), the carrier OS thread is <em>immediately released</em> and picks up another virtual thread — no OS thread wastage.<br><br><strong>Impact on backend:</strong> the classic scaling bottleneck (thread-per-request blocking I/O) disappears <em>without</em> reactive programming complexity. Spring Boot 3.2+: enable with <code>spring.threads.virtual.enabled=true</code>.<br><br><strong>Traditional platform thread example (limited scalability):</strong><br><pre>// Before: Platform threads (OS threads)\nExecutorService executor = Executors.newFixedThreadPool(100);\n\nfor (int i = 0; i &lt; 10_000; i++) {\n  executor.submit(() -&gt; {\n    // Blocking I/O - wastes OS thread while waiting\n    String data = httpClient.get(\"https://api.example.com/data\");\n    database.save(data);  // blocks again\n  });\n}\n// Problem: Only 100 requests can run concurrently\n// Other 9,900 requests wait in queue\n// Creating 10,000 OS threads → OutOfMemoryError</pre><br><strong>Virtual thread example (millions of threads):</strong><br><pre>// Java 21: Virtual threads\ntry (var executor = Executors.newVirtualThreadPerTaskExecutor()) {\n  for (int i = 0; i &lt; 10_000; i++) {\n    executor.submit(() -&gt; {\n      // Blocking I/O - carrier thread released automatically\n      String data = httpClient.get(\"https://api.example.com/data\");\n      database.save(data);  // carrier thread released again\n    });\n  }\n}\n// Success: All 10,000 virtual threads run concurrently\n// Only ~10 OS carrier threads used\n// Can scale to millions of virtual threads</pre><br><strong>Creating virtual threads:</strong><br><pre>// Method 1: Thread.ofVirtual()\nThread vThread = Thread.ofVirtual().start(() -&gt; {\n  System.out.println(\"Running on virtual thread\");\n});\n\n// Method 2: Thread.startVirtualThread()\nThread.startVirtualThread(() -&gt; {\n  System.out.println(\"Virtual thread shorthand\");\n});\n\n// Method 3: Virtual thread executor (recommended)\ntry (var executor = Executors.newVirtualThreadPerTaskExecutor()) {\n  Future&lt;String&gt; future = executor.submit(() -&gt; {\n    Thread.sleep(1000);  // non-blocking for carrier thread\n    return \"Result\";\n  });\n  String result = future.get();\n}</pre><br><strong>Spring Boot integration:</strong><br><pre>// application.properties\nspring.threads.virtual.enabled=true\n\n// Or programmatically\n@Configuration\npublic class ThreadConfig {\n  @Bean\n  public TomcatProtocolHandlerCustomizer&lt;?&gt; protocolHandlerVirtualThreadExecutorCustomizer() {\n    return protocolHandler -&gt; {\n      protocolHandler.setExecutor(Executors.newVirtualThreadPerTaskExecutor());\n    };\n  }\n}\n\n// Now every HTTP request runs on a virtual thread\n@RestController\npublic class ApiController {\n  @GetMapping(\"/users/{id}\")\n  public User getUser(@PathVariable Long id) {\n    // Blocking JDBC call - no problem with virtual threads\n    User user = userRepository.findById(id);\n    // Blocking HTTP call - carrier thread released\n    Address addr = addressService.fetchAddress(user.getId());\n    return user;\n  }\n}</pre><br><strong>Real-world performance example:</strong><br><pre>// Simulating 10,000 concurrent API calls with 1s latency each\n\n// Platform threads: 100 thread pool\nlong start = System.currentTimeMillis();\ntry (var executor = Executors.newFixedThreadPool(100)) {\n  for (int i = 0; i &lt; 10_000; i++) {\n    executor.submit(() -&gt; Thread.sleep(1000));\n  }\n}\nlong duration = System.currentTimeMillis() - start;\nSystem.out.println(\"Platform threads: \" + duration + \"ms\");\n// Output: ~100 seconds (10,000 tasks / 100 threads)\n\n// Virtual threads\nstart = System.currentTimeMillis();\ntry (var executor = Executors.newVirtualThreadPerTaskExecutor()) {\n  for (int i = 0; i &lt; 10_000; i++) {\n    executor.submit(() -&gt; Thread.sleep(1000));\n  }\n}\nduration = System.currentTimeMillis() - start;\nSystem.out.println(\"Virtual threads: \" + duration + \"ms\");\n// Output: ~1 second (all run concurrently)</pre><br><strong>Caveats:</strong><ul><li><strong>CPU-bound tasks:</strong> Still need OS threads — virtual threads help with I/O, not CPU</li><li><strong>synchronized blocks:</strong> Can pin the carrier thread — use <code>ReentrantLock</code> instead</li><li><strong>Thread locals:</strong> Work but expensive — avoid for per-request data, use scoped values (Java 21 preview)</li><li><strong>Thread pools:</strong> Don't pool virtual threads — they're cheap to create</li></ul><br><strong>When to use:</strong><ul><li>✅ High-concurrency I/O-bound applications (web servers, microservices)</li><li>✅ Blocking JDBC, HTTP clients, file I/O</li><li>✅ Simplify code — no need for reactive/async programming</li><li>❌ CPU-intensive computations (use platform threads or parallel streams)</li></ul>"},

    {q:"What is the String Pool and how does string interning work?",d:"medium",
     a:"The <strong>String Pool</strong> (part of the heap since Java 7) is a cache of unique <code>String</code> instances. When you write a string literal (<code>\"hello\"</code>), the JVM checks the pool: if the value exists, it returns the existing reference; otherwise it creates a new entry.<br><br><code>new String(\"hello\")</code> always creates a new heap object, <em>bypassing</em> the pool.<br><br><code>String.intern()</code> manually places a String in the pool and returns the canonical reference:<br><pre>String a = new String(\"hello\");\nString b = a.intern();\nb == \"hello\" // true — same pooled instance</pre><br><strong>Practical use:</strong> interning is useful when you store millions of repeated string values (e.g., enum-like strings from a database) to reduce heap pressure. The pool is garbage-collected in modern JVMs so unused interned strings are freed."},

    {q:"Explain method hiding vs method overriding in Java.",d:"hard",
     a:"<strong>Method overriding</strong> applies to <em>instance methods</em>. When a subclass defines an instance method with the same signature, the JVM calls the subclass version based on the <em>runtime type</em> of the object — this is dynamic dispatch / polymorphism.<br><br><strong>Method hiding</strong> applies to <em>static methods</em>. When a subclass defines a static method with the same signature, the version called depends on the <em>reference type</em> at compile time, not the runtime type.<br><pre>Animal a = new Dog();\na.speak();         // Dog.speak() — overriding (runtime type)\na.staticMethod();  // Animal.staticMethod() — hiding (compile-time type)</pre><br><strong>Private methods</strong> are not overridden — each class has its own private copy. <code>@Override</code> on a private method in a subclass creates a new unrelated method (no annotation error but also no polymorphism)."}
  ]
};

SEC_JAVA.qs.push(
  {q:"What is the difference between Runnable and Callable?",d:"easy",
   a:"<strong>Runnable:</strong> functional interface with <code>void run()</code> — cannot return a value or throw checked exceptions. Used with <code>Thread</code> or <code>ExecutorService.submit()</code>.<br><br><strong>Callable&lt;V&gt;:</strong> functional interface with <code>V call() throws Exception</code> — can return a value and throw checked exceptions. Used with <code>ExecutorService.submit()</code> which returns a <code>Future&lt;V&gt;</code>.<br><br><strong>Key differences:</strong><br><table><tr><td></td><td><strong>Runnable</strong></td><td><strong>Callable&lt;V&gt;</strong></td></tr><tr><td>Method</td><td>void run()</td><td>V call() throws Exception</td></tr><tr><td>Return value</td><td>❌ Cannot return</td><td>✅ Returns V</td></tr><tr><td>Checked exceptions</td><td>❌ Cannot throw</td><td>✅ Can throw</td></tr><tr><td>Use case</td><td>Fire-and-forget tasks</td><td>Tasks that return results</td></tr></table><br><strong>Runnable example (no return value):</strong><br><pre>// Creating Runnable\nRunnable task = () -&gt; {\n  System.out.println(\"Running on thread: \" + Thread.currentThread().getName());\n  // Cannot return a value\n  // Cannot throw checked exceptions\n};\n\n// Using with Thread\nThread thread = new Thread(task);\nthread.start();\n\n// Using with ExecutorService\nExecutorService executor = Executors.newFixedThreadPool(5);\nexecutor.submit(task);  // returns Future&lt;?&gt; but result is always null\nexecutor.execute(task); // fire-and-forget, no Future returned\n\n// Real-world example: logging, cleanup\nRunnable logger = () -&gt; {\n  for (int i = 0; i &lt; 5; i++) {\n    System.out.println(\"Log entry \" + i);\n    Thread.sleep(100);  // must catch InterruptedException\n  }\n};\nnew Thread(logger).start();</pre><br><strong>Callable example (returns value):</strong><br><pre>// Creating Callable\nCallable&lt;Integer&gt; task = () -&gt; {\n  Thread.sleep(2000);  // can throw checked exception\n  return 42;  // returns a value\n};\n\n// Using with ExecutorService\nExecutorService executor = Executors.newFixedThreadPool(5);\nFuture&lt;Integer&gt; future = executor.submit(task);\n\n// Do other work while task runs in background\nSystem.out.println(\"Task submitted, doing other work...\");\n\n// Get result (blocks if not ready)\nInteger result = future.get();  // blocks until done\nSystem.out.println(\"Result: \" + result);  // 42\n\n// Check if done without blocking\nif (future.isDone()) {\n  Integer value = future.get();  // won't block\n}\n\n// Get with timeout\ntry {\n  Integer value = future.get(1, TimeUnit.SECONDS);\n} catch (TimeoutException e) {\n  future.cancel(true);  // cancel if taking too long\n}</pre><br><strong>Real-world Callable examples:</strong><br><pre>// 1. Fetch data from API\nCallable&lt;String&gt; fetchData = () -&gt; {\n  HttpClient client = HttpClient.newHttpClient();\n  HttpResponse&lt;String&gt; response = client.send(request, BodyHandlers.ofString());\n  return response.body();  // returns the data\n};\n\n// 2. Calculate expensive value\nCallable&lt;Double&gt; calculate = () -&gt; {\n  double result = 0;\n  for (int i = 0; i &lt; 1_000_000; i++) {\n    result += Math.sqrt(i);\n  }\n  return result;\n};\n\n// 3. Database query\nCallable&lt;List&lt;User&gt;&gt; queryUsers = () -&gt; {\n  try (Connection conn = dataSource.getConnection()) {\n    PreparedStatement stmt = conn.prepareStatement(\"SELECT * FROM users\");\n    ResultSet rs = stmt.executeQuery();\n    List&lt;User&gt; users = new ArrayList&lt;&gt;();\n    while (rs.next()) {\n      users.add(new User(rs.getString(\"name\"), rs.getInt(\"age\")));\n    }\n    return users;\n  }  // can throw SQLException - Callable allows this\n};</pre><br><strong>Multiple Callables with invokeAll/invokeAny:</strong><br><pre>ExecutorService executor = Executors.newFixedThreadPool(3);\n\n// Create multiple tasks\nList&lt;Callable&lt;Integer&gt;&gt; tasks = List.of(\n  () -&gt; { Thread.sleep(1000); return 1; },\n  () -&gt; { Thread.sleep(2000); return 2; },\n  () -&gt; { Thread.sleep(3000); return 3; }\n);\n\n// invokeAll - wait for all tasks\nList&lt;Future&lt;Integer&gt;&gt; futures = executor.invokeAll(tasks);\nfor (Future&lt;Integer&gt; f : futures) {\n  System.out.println(f.get());  // 1, 2, 3\n}\n\n// invokeAny - return first completed\nInteger firstResult = executor.invokeAny(tasks);\nSystem.out.println(firstResult);  // 1 (fastest)\n// Other tasks are cancelled</pre><br><strong>Converting Runnable to Callable:</strong><br><pre>// Runnable to Callable with fixed result\nRunnable runnable = () -&gt; System.out.println(\"Hello\");\nCallable&lt;Void&gt; callable = Executors.callable(runnable, null);\n\n// Or use adapter\nCallable&lt;String&gt; adapted = () -&gt; {\n  runnable.run();\n  return \"Done\";\n};</pre><br><strong>When to use each:</strong><br><ul><li><strong>Runnable:</strong> Background tasks, logging, cleanup, no result needed, simpler code</li><li><strong>Callable:</strong> Need return value, need exception propagation, parallel computations with results</li></ul>"},

  {q:"What is the difference between final, finally, and finalize()?",d:"easy",
   a:"Three completely unrelated keywords that often appear together in interviews:<br><br><strong>final:</strong> keyword with three uses:<ul><li><code>final int x = 5</code> — variable cannot be reassigned</li><li><code>final class String</code> — class cannot be extended</li><li><code>final void method()</code> — method cannot be overridden in subclasses</li></ul><br><strong>finally:</strong> block in try-catch-finally. Always executes after try/catch — even if an exception was thrown. Used to release resources (though try-with-resources is now preferred).<br><pre>try { conn.execute(); }\ncatch (Exception e) { log.error(e); }\nfinally { conn.close(); } // always runs</pre><br><strong>finalize():</strong> method called by the GC before collecting an object. Deprecated since Java 9, removed in Java 18. Unreliable and problematic. Use <code>Cleaner</code> or try-with-resources instead."},

  {q:"What is the difference between an Iterator and a ListIterator?",d:"medium",
   a:"<strong>Iterator:</strong> can traverse any <code>Collection</code> (not just lists). Supports forward-only traversal. Methods: <code>hasNext()</code>, <code>next()</code>, <code>remove()</code> (optional).<br><br><strong>ListIterator:</strong> extends Iterator, only works with <code>List</code> implementations. Adds bidirectional traversal and modification:<ul><li><code>hasPrevious()</code>, <code>previous()</code> — traverse backwards</li><li><code>add(e)</code> — insert element at current position</li><li><code>set(e)</code> — replace last returned element</li><li><code>nextIndex()</code>, <code>previousIndex()</code> — get current position</li></ul><br>Use Iterator for general collection traversal; ListIterator when you need to traverse backwards or modify a list while iterating."},

  {q:"What is covariance and contravariance in Java generics?",d:"hard",
   a:"<strong>Covariance (<code>&lt;? extends T&gt;</code> — upper bounded wildcard):</strong> accept T or any subtype. Can <em>read</em> from the structure but not write (except null).<br><pre>List&lt;? extends Number&gt; nums = new ArrayList&lt;Integer&gt;();\nNumber n = nums.get(0); // OK\nnums.add(42); // COMPILE ERROR</pre><br><strong>Contravariance (<code>&lt;? super T&gt;</code> — lower bounded wildcard):</strong> accept T or any supertype. Can <em>write</em> T into the structure but reading gives Object.<br><pre>List&lt;? super Integer&gt; list = new ArrayList&lt;Number&gt;();\nlist.add(42); // OK\nObject o = list.get(0); // only Object</pre><br><strong>PECS principle</strong> (Producer Extends, Consumer Super):<ul><li>If you're reading FROM the collection (producer) → use <code>extends</code></li><li>If you're writing INTO the collection (consumer) → use <code>super</code></li></ul>Example: <code>Collections.copy(List&lt;? super T&gt; dest, List&lt;? extends T&gt; src)</code>"},

  {q:"What is the difference between deep copy and shallow copy?",d:"medium",
   a:"<strong>Shallow copy:</strong> creates a new object with copied field values. For reference-type fields, copies the reference — both original and copy point to the same nested objects.<br><pre>class Order {\n  List&lt;Item&gt; items; // reference field\n}\nOrder copy = new Order();\ncopy.items = original.items; // same list!\ncopy.items.add(newItem);     // modifies original too!</pre><br><strong>Deep copy:</strong> recursively copies all nested objects — fully independent copy.<br><pre>Order deepCopy = new Order();\ndeepCopy.items = new ArrayList&lt;&gt;(original.items); // new list\n// For mutable items, copy each item too</pre><br><strong>Java mechanisms for deep copy:</strong><ul><li>Manual — implement copy constructors or factory methods</li><li>Serialization — serialize to bytes, deserialize to new object (slow)</li><li>Cloneable + override <code>clone()</code> (error-prone, avoid)</li><li>Library — ModelMapper, MapStruct, Jackson (serialize/deserialize)</li></ul><br>Prefer explicit copy constructors or builders over <code>Cloneable</code>."},

  {q:"What are sealed interfaces and pattern matching for switch in Java 21?",d:"hard",
   a:"<strong>Sealed interfaces</strong> restrict which classes can implement them — you control the complete hierarchy:<br><pre>sealed interface Shape permits Circle, Rectangle, Triangle {}\nrecord Circle(double radius) implements Shape {}\nrecord Rectangle(double w, double h) implements Shape {}\nrecord Triangle(double base, double h) implements Shape {}</pre><br><strong>Pattern matching for switch</strong> (stable Java 21) uses sealed types for exhaustive switching:<br><pre>double area = switch (shape) {\n  case Circle c    -&gt; Math.PI * c.radius() * c.radius();\n  case Rectangle r -&gt; r.w() * r.h();\n  case Triangle t  -&gt; 0.5 * t.base() * t.h();\n  // NO default needed — compiler knows all subtypes\n};</pre><br><strong>Guarded patterns:</strong><br><pre>case Circle c when c.radius() &gt; 100 -&gt; \"large circle\";\ncase Circle c -&gt; \"small circle\";</pre><br>This enables functional-style algebraic data types in Java — a type-safe alternative to instanceof chains and class hierarchies."}
);

SEC_JAVA.qs.push(
  {q:"What is pattern matching for instanceof (Java 16+)?",d:"easy",
   a:"Before Java 16, checking and casting required two steps:<br><pre>// Old way\nif (obj instanceof String) {\n  String s = (String) obj; // redundant cast\n  System.out.println(s.length());\n}</pre><br><strong>Pattern matching</strong> combines the check and binding into one expression:<br><pre>// Java 16+\nif (obj instanceof String s) {\n  System.out.println(s.length()); // s is already String\n}\n\n// Works with negation too\nif (!(obj instanceof String s)) {\n  throw new IllegalArgumentException();\n}\n// s is in scope here too!</pre><br><strong>In switch expressions (Java 21):</strong><br><pre>String result = switch (obj) {\n  case Integer i -> \"int: \" + i;\n  case String s  -> \"str: \" + s;\n  case null      -> \"null\";\n  default        -> \"other\";\n};</pre><br>Eliminates entire classes of ClassCastException and makes code significantly more readable."},

  {q:"What are text blocks in Java 15+?",d:"easy",
   a:"Text blocks provide a cleaner syntax for multi-line string literals — no more <code>\\n</code>, no more string concatenation for JSON/SQL/HTML.<br><br><pre>// Old way — messy\nString json = \"{\\n\" +\n  \"  \\\"name\\\": \\\"Alice\\\",\\n\" +\n  \"  \\\"age\\\": 30\\n\" +\n\"}\";\n\n// Text block\nString json = \"\"\"\n    {\n      \"name\": \"Alice\",\n      \"age\": 30\n    }\n    \"\"\";</pre><br><strong>Rules:</strong><ul><li>Opening <code>\"\"\"</code> must be followed by a newline</li><li>Closing <code>\"\"\"</code> position determines indentation stripping — common leading whitespace is removed</li><li>Trailing newline is included unless closing <code>\"\"\"</code> is on same line as last content</li></ul><br><strong>Escape sequences:</strong><ul><li><code>\\n</code> — still works for explicit newlines</li><li><code>\\</code> at end of line — suppresses newline (join lines)</li><li><code>\\s</code> — explicit space (prevents trailing whitespace trimming)</li></ul><br><strong>Useful for:</strong> SQL queries, JSON payloads in tests, HTML templates, GraphQL queries. Works seamlessly with <code>String.formatted()</code>:<br><pre>String query = \"\"\"\n    SELECT * FROM users\n    WHERE id = %d\n    \"\"\".formatted(userId);</pre>"},

  {q:"What is var (local variable type inference) in Java 10+?",d:"easy",
   a:"<code>var</code> lets the compiler infer the type of a local variable from its initializer — reducing boilerplate without losing static typing.<br><br><pre>// Without var\nMap&lt;String, List&lt;Order&gt;&gt; ordersByCustomer = new HashMap&lt;&gt;();\nBufferedReader reader = new BufferedReader(new FileReader(\"file.txt\"));\n\n// With var\nvar ordersByCustomer = new HashMap&lt;String, List&lt;Order&gt;&gt;();\nvar reader = new BufferedReader(new FileReader(\"file.txt\"));</pre><br><strong>Restrictions — var CANNOT be used for:</strong><ul><li>Method parameters or return types</li><li>Fields</li><li>Variables without an initializer: <code>var x;</code> — illegal</li><li>Lambda parameters (use explicit type or <code>_</code> in Java 21)</li><li><code>var x = null;</code> — compiler can't infer type from null</li></ul><br><strong>Best practice:</strong> use <code>var</code> when the type is obvious from the right-hand side (constructor, factory method). Avoid when it reduces clarity:<br><pre>var result = process(); // unclear — what type is result?\nvar list = new ArrayList&lt;String&gt;(); // clear</pre>"},

  {q:"How does try-with-resources work and what is AutoCloseable?",d:"medium",
   a:"Try-with-resources automatically closes resources declared in the <code>try()</code> header — guaranteeing <code>close()</code> is called even if an exception is thrown.<br><br><pre>// Old way — verbose and error-prone\nConnection conn = null;\ntry {\n  conn = dataSource.getConnection();\n  // use conn\n} finally {\n  if (conn != null) conn.close(); // easy to forget\n}\n\n// Try-with-resources\ntry (Connection conn = dataSource.getConnection();\n     PreparedStatement ps = conn.prepareStatement(sql)) {\n  // use conn and ps\n} // both closed automatically in reverse order</pre><br><strong>AutoCloseable:</strong> any class implementing <code>AutoCloseable</code> (one method: <code>void close() throws Exception</code>) can be used. <code>Closeable</code> extends it with <code>IOException</code>.<br><br><strong>Suppressed exceptions:</strong> if both the body and <code>close()</code> throw, the close exception is <em>suppressed</em> (attached to the primary exception via <code>getSuppressed()</code>) — the original exception is not lost.<br><br><strong>Java 9 enhancement:</strong> you can use effectively-final variables declared outside: <code>try (conn) { ... }</code>"},

  {q:"What are the different types of nested classes in Java?",d:"medium",
   a:"Java has four kinds of nested classes:<br><br><strong>Static nested class:</strong> declared <code>static</code> inside an outer class. No reference to the outer instance. Acts like a regular top-level class that's namespaced.<br><pre>class Outer {\n  static class StaticNested { ... } // no Outer.this\n}</pre><br><strong>Inner class (non-static):</strong> implicitly holds a reference to the enclosing outer instance. Can access outer's private members.<br><pre>class Outer {\n  class Inner { \n    void foo() { Outer.this.field; } // outer reference\n  }\n}</pre><br><strong>Local class:</strong> defined inside a method. Can access effectively-final locals.<br><br><strong>Anonymous class:</strong> unnamed class defined and instantiated in one expression. Common before lambdas.<br><pre>Runnable r = new Runnable() {\n  public void run() { ... }\n};</pre><br><strong>Memory leak risk:</strong> non-static inner classes and anonymous classes holding an outer reference prevent GC of the outer object. Prefer static nested classes unless you genuinely need the outer reference.<br><br><strong>Java 16+ records and interfaces</strong> can be declared static nested."},

  {q:"What is the Enum type and what can it do beyond simple constants?",d:"medium",
   a:"Enums in Java are full classes — each constant is a singleton instance of the enum class.<br><br><strong>Beyond simple constants — fields, methods, constructors:</strong><br><pre>public enum HttpStatus {\n  OK(200, \"OK\"),\n  NOT_FOUND(404, \"Not Found\"),\n  INTERNAL_ERROR(500, \"Internal Server Error\");\n\n  private final int code;\n  private final String reason;\n\n  HttpStatus(int code, String reason) {\n    this.code = code;\n    this.reason = reason;\n  }\n\n  public int getCode() { return code; }\n\n  public static HttpStatus fromCode(int code) {\n    return Arrays.stream(values())\n      .filter(s -&gt; s.code == code)\n      .findFirst()\n      .orElseThrow();\n  }\n}</pre><br><strong>Abstract methods per constant:</strong><br><pre>enum Operation {\n  ADD { public int apply(int a, int b) { return a + b; } },\n  MUL { public int apply(int a, int b) { return a * b; } };\n  public abstract int apply(int a, int b);\n}</pre><br><strong>EnumSet and EnumMap:</strong> highly efficient collections backed by bit vectors/arrays — much faster than HashSet/HashMap for enum keys."},

  {q:"What is the difference between Iterator's remove() and Collection's remove()?",d:"medium",
   a:"Modifying a collection while iterating over it with a for-each loop throws <code>ConcurrentModificationException</code> (fail-fast iterators):<br><br><pre>// WRONG — throws ConcurrentModificationException\nfor (String s : list) {\n  if (s.startsWith(\"x\")) list.remove(s);\n}</pre><br><strong>Safe removal during iteration — Iterator.remove():</strong><br><pre>Iterator&lt;String&gt; it = list.iterator();\nwhile (it.hasNext()) {\n  if (it.next().startsWith(\"x\")) {\n    it.remove(); // removes last element returned by next()\n  }\n}</pre><br><strong>Java 8+ — removeIf() (cleanest):</strong><br><pre>list.removeIf(s -&gt; s.startsWith(\"x\"));</pre><br><strong>Key rules for Iterator.remove():</strong><ul><li>Must call <code>next()</code> before <code>remove()</code></li><li>Can only call <code>remove()</code> once per <code>next()</code> call</li><li>Not all iterators support remove (throws <code>UnsupportedOperationException</code> for unmodifiable collections)</li></ul>"},

  {q:"What are method references and what are the four kinds?",d:"medium",
   a:"Method references are a shorthand for lambdas that simply call an existing method. Syntax: <code>ClassName::methodName</code><br><br><strong>1. Static method reference:</strong><br><pre>// Lambda:  (x) -> Integer.parseInt(x)\n// Ref:     Integer::parseInt\nlist.stream().map(Integer::parseInt)</pre><br><strong>2. Instance method of a particular object:</strong><br><pre>// Lambda:  (x) -> myPrinter.print(x)\n// Ref:     myPrinter::print\nlist.forEach(myPrinter::print)</pre><br><strong>3. Instance method of an arbitrary object of a type:</strong><br><pre>// Lambda:  (s) -> s.toUpperCase()\n// Ref:     String::toUpperCase\nlist.stream().map(String::toUpperCase)</pre><br><strong>4. Constructor reference:</strong><br><pre>// Lambda:  (s) -> new StringBuilder(s)\n// Ref:     StringBuilder::new\nlist.stream().map(StringBuilder::new)</pre><br><strong>When to use:</strong> method references are more readable than lambdas when the lambda does nothing but delegate to a single method. If you need to transform arguments or combine calls, use a lambda instead."},

  {q:"What is the Collections utility class and its most important methods?",d:"easy",
   a:"<code>java.util.Collections</code> provides static utility methods for collection operations:<br><br><strong>Sorting & searching:</strong><br><pre>Collections.sort(list);                          // natural order\nCollections.sort(list, comparator);              // custom order\nCollections.binarySearch(sortedList, key);       // O(log n)\nCollections.reverse(list);\nCollections.shuffle(list);                       // random order\nCollections.swap(list, i, j);</pre><br><strong>Min/Max:</strong><br><pre>Collections.min(collection);\nCollections.max(collection, comparator);</pre><br><strong>Frequency & disjoint:</strong><br><pre>Collections.frequency(collection, element);      // count occurrences\nCollections.disjoint(c1, c2);                    // true if no common elements</pre><br><strong>Unmodifiable wrappers:</strong><br><pre>List&lt;String&gt; immutable = Collections.unmodifiableList(list);\n// throws UnsupportedOperationException on mutation\n\n// Java 9+ — prefer these (truly immutable):\nList.of(\"a\", \"b\");\nSet.of(\"x\", \"y\");\nMap.of(\"k\", \"v\");</pre><br><strong>Singleton/empty collections:</strong><br><pre>Collections.singletonList(\"only\"); // immutable single-element list\nCollections.emptyList();           // shared empty instance, no allocation</pre>"}
);

SEC_JAVA.qs.push(
);

SEC_JAVA.qs.push(
  {q:"What is the difference between String.format(), formatted(), and MessageFormat?",d:"easy",
   a:"<strong>String.format()</strong>: classic printf-style formatting. Returns a new String.<br><pre>String s = String.format(\"Hello %s, you are %d years old\", name, age);\nString money = String.format(\"%.2f\", 1234.5678); // \"1234.57\"</pre><br><strong>String.formatted()</strong> (Java 15+): instance method version — same as <code>String.format()</code> but called on the format string. Works great with text blocks:<br><pre>String msg = \"Order %s placed for %s\".formatted(orderId, customerName);\n\nString body = \"\"\"\n  Dear %s,\n  Your order %s is confirmed.\n  \"\"\".formatted(name, orderId);</pre><br><strong>MessageFormat:</strong> better for internationalisation — handles pluralisation and locale-aware formatting:<br><pre>String pattern = \"At {0,time} on {0,date}, there were {1,number} visitors.\";\nString result = MessageFormat.format(pattern, new Date(), 42);</pre><br><strong>Common format specifiers:</strong> <code>%s</code> (string), <code>%d</code> (integer), <code>%f</code> (float), <code>%n</code> (newline), <code>%b</code> (boolean), <code>%x</code> (hex), <code>%05d</code> (zero-padded)."},

  {q:"What are default methods in interfaces and what problem do they solve?",d:"medium",
   a:"Default methods (Java 8) allow interfaces to have concrete method implementations without breaking existing implementors — enabling API evolution.<br><br><strong>Problem they solve:</strong> before Java 8, adding a new method to an interface broke all existing implementations. With default methods, the interface provides a default implementation that subclasses can optionally override.<br><pre>interface Collection&lt;E&gt; {\n  // New in Java 8 — existing implementations get this for free\n  default Stream&lt;E&gt; stream() {\n    return StreamSupport.stream(spliterator(), false);\n  }\n\n  default void forEach(Consumer&lt;? super E&gt; action) {\n    for (E e : this) action.accept(e);\n  }\n}</pre><br><strong>Diamond problem resolution:</strong> if a class implements two interfaces with the same default method, it <em>must</em> override it to resolve ambiguity:<br><pre>class MyClass implements A, B {\n  public void hello() {\n    A.super.hello(); // explicitly choose which default\n  }\n}</pre><br><strong>Rule:</strong> class always wins over interface; more specific interface wins over less specific."},

  {q:"How does hashCode() contract work and what breaks it silently in a HashMap?",d:"hard",
   a:"The hashCode contract (from Object.hashCode() javadoc):<br><br><strong>1. Consistency:</strong> if <code>equals()</code> doesn't change, <code>hashCode()</code> must return the same value across multiple calls.<br><strong>2. Equality implies same hashCode:</strong> if <code>a.equals(b)</code>, then <code>a.hashCode() == b.hashCode()</code> MUST be true.<br><strong>3. Not required reverse:</strong> different objects can have the same hashCode (collisions are allowed).<br><br><strong>Silent breakage — mutable key:</strong><br><pre>class User {\n  String name;\n  @Override public int hashCode() { return name.hashCode(); }\n  @Override public boolean equals(Object o) { ... }\n}\n\nHashMap&lt;User, Order&gt; orders = new HashMap&lt;&gt;();\nUser user = new User(\"Alice\");\norders.put(user, order);\nuser.name = \"Bob\"; // MUTATION after insert\norders.get(user);  // returns null — user now hashes to wrong bucket!</pre><br><strong>Rule:</strong> never mutate fields used in <code>hashCode()</code>/<code>equals()</code> after inserting into a hash-based collection. Use immutable keys or only immutable fields (final, computed once)."},

  {q:"What is ClassLoader and how does parent delegation work?",d:"hard",
   a:"A ClassLoader loads <code>.class</code> files into the JVM. Java uses a hierarchy of ClassLoaders with <strong>parent delegation</strong> — before loading a class itself, a ClassLoader first asks its parent.<br><br><strong>Bootstrap ClassLoader (native C):</strong> loads core JDK classes from <code>rt.jar</code> or <code>java.base</code> module (String, Object, etc.).<br><br><strong>Extension/Platform ClassLoader:</strong> loads JDK extensions (<code>javax.*</code>, <code>java.sql.*</code>).<br><br><strong>Application ClassLoader:</strong> loads classes from the classpath (your app code, dependencies).<br><br><strong>Parent delegation flow:</strong><br><pre>1. Check if class is already loaded (findLoadedClass)\n2. Delegate to parent ClassLoader first\n3. If parent returns null, only then load it yourself</pre><br><strong>Why parent delegation?</strong> prevents a malicious <code>java.lang.String</code> on the classpath from replacing the JDK's String — the Bootstrap ClassLoader always wins.<br><br><strong>Custom ClassLoaders:</strong> web containers (Tomcat) use separate ClassLoaders per WAR for isolation. OSGi uses a custom non-delegating model for modularity."},

  {q:"What is the Integer cache and when does == on Integer return true?",d:"medium",
   a:"Java caches <code>Integer</code> objects for values in the range <code>-128 to +127</code> by default (via <code>Integer.valueOf()</code>). This means <code>==</code> will return <code>true</code> for cached values but <code>false</code> outside the range.<br><pre>Integer a = 100, b = 100;\nSystem.out.println(a == b);  // true (cached)\n\nInteger c = 200, d = 200;\nSystem.out.println(c == d);  // false (not cached, different objects)\n\nc.equals(d);  // true — always use .equals() for Integer comparison</pre><br><strong>Why the cache exists:</strong> small integers are used extremely frequently (loop counters, status codes) — caching reduces object allocation overhead.<br><br><strong>Cache range is configurable:</strong> <code>-XX:AutoBoxCacheMax=1000</code> extends the upper limit to 1000.<br><br><strong>Best practice:</strong> NEVER use <code>==</code> for <code>Integer</code>, <code>Long</code>, or any boxed primitive comparison in production code — always use <code>.equals()</code>. <code>==</code> checks reference identity, not value equality."},

  {q:"What is the difference between Serializable and Externalizable?",d:"hard",
   a:"Both allow objects to be converted to a byte stream (for file I/O, network, caching), but <code>Externalizable</code> gives you full control over the format.<br><br><strong>Serializable (automatic, declarative):</strong><br><pre>public class User implements Serializable {\n  private String name;\n  private transient String password; // excluded from serialization\n}</pre>The JVM uses reflection to serialize all non-transient fields. Override <code>writeObject()</code> and <code>readObject()</code> for custom behavior.<br><br><strong>Externalizable (manual, explicit):</strong><br><pre>public class User implements Externalizable {\n  private String name;\n  private String password;\n\n  @Override\n  public void writeExternal(ObjectOutput out) throws IOException {\n    out.writeUTF(name);\n    // password not written — manual control\n  }\n\n  @Override\n  public void readExternal(ObjectInput in) throws IOException {\n    name = in.readUTF();\n  }\n}</pre><br><strong>Key difference:</strong> Externalizable requires you to write every field explicitly — better performance (no reflection), smaller output, but more error-prone. <strong>Externalizable requires a public no-arg constructor.</strong><br><br><strong>Modern alternative:</strong> Jackson, Protocol Buffers, or JSON serialization — avoid Java serialization for security reasons (deserialization gadgets)."},

  {q:"What is transient and volatile — when do you use each?",d:"medium",
   a:"<strong>transient:</strong> excludes a field from Java serialization.<br><pre>class Session implements Serializable {\n  private String userId;\n  private transient Socket connection; // not serializable, skip it\n}</pre>When deserializing, transient fields are set to their default value (null for objects, 0 for primitives). Reconstruct them manually in <code>readObject()</code> if needed.<br><br><strong>volatile:</strong> tells the JVM that a field may be modified by multiple threads — disables caching, ensures visibility.<br><pre>class Worker {\n  private volatile boolean stopRequested;\n\n  public void run() {\n    while (!stopRequested) { ... } // always reads fresh value\n  }\n\n  public void shutdown() { stopRequested = true; } // visible to run() immediately\n}</pre><br><strong>What volatile does NOT do:</strong> it does NOT provide atomicity for compound operations like <code>count++</code>. Use <code>AtomicInteger</code> or synchronization for that.<br><br><strong>When to use volatile:</strong> simple flags (shutdown signal, status), double-checked locking (with proper pattern). For complex logic, use proper synchronization or <code>java.util.concurrent</code> utilities."},

  {q:"What are the most important Java 8-25 features a senior dev should know?",d:"medium",
   a:"<strong>Java 8 (2014 - Foundational):</strong><ul><li><strong>Lambdas & Method References:</strong> <code>(x, y) -&gt; x + y</code>, <code>String::length</code></li><li><strong>Streams API:</strong> declarative data processing — <code>list.stream().filter().map().collect()</code></li><li><strong>Optional:</strong> explicit null handling — <code>Optional.ofNullable(value).orElse(default)</code></li><li><strong>Default methods in interfaces:</strong> <code>default void log() { ... }</code></li><li><strong>Date/Time API:</strong> <code>LocalDate</code>, <code>LocalDateTime</code>, <code>ZonedDateTime</code> (replaced Date/Calendar)</li><li><strong>Functional interfaces:</strong> <code>Predicate</code>, <code>Function</code>, <code>Consumer</code>, <code>Supplier</code></li></ul><br><strong>Java 9 (2017):</strong><ul><li><strong>Module system (Jigsaw):</strong> <code>module-info.java</code></li><li><strong>Factory methods for collections:</strong> <code>List.of()</code>, <code>Set.of()</code>, <code>Map.of()</code></li><li><strong>Private methods in interfaces</strong></li><li><strong>Try-with-resources enhancement:</strong> effectively-final variables</li><li><strong>Stream enhancements:</strong> <code>takeWhile()</code>, <code>dropWhile()</code>, <code>ofNullable()</code></li></ul><br><strong>Java 10 (2018):</strong><ul><li><strong>Local variable type inference:</strong> <code>var list = new ArrayList&lt;String&gt;();</code></li><li><strong>Parallel full GC for G1</strong></li></ul><br><strong>Java 11 (LTS - 2018):</strong><ul><li><strong>var in lambda parameters:</strong> <code>(var x, var y) -&gt; x + y</code></li><li><strong>String methods:</strong> <code>isBlank()</code>, <code>lines()</code>, <code>strip()</code>, <code>repeat(n)</code></li><li><strong>HttpClient API (standard):</strong> async, HTTP/2 support</li><li><strong>Files enhancements:</strong> <code>readString()</code>, <code>writeString()</code></li><li><strong>Removed Java EE modules</strong> (must add dependencies manually)</li></ul><br><strong>Java 14 (2020):</strong><ul><li><strong>Switch expressions (stable):</strong> <code>int x = switch(day) { case MON -> 1; default -> 0; }</code></li><li><strong>Records (preview):</strong> <code>record Point(int x, int y) {}</code></li><li><strong>NullPointerException messages:</strong> shows which variable was null</li></ul><br><strong>Java 15 (2020):</strong><ul><li><strong>Text blocks (stable):</strong> <code>\"\"\" multi-line string \"\"\"</code></li><li><strong>Sealed classes (preview):</strong> restrict subclasses</li></ul><br><strong>Java 16 (2021):</strong><ul><li><strong>Pattern matching for instanceof (stable):</strong> <code>if (obj instanceof String s) { ... }</code></li><li><strong>Records (stable)</strong></li><li><strong>Stream.toList():</strong> simpler than <code>collect(Collectors.toList())</code></li></ul><br><strong>Java 17 (LTS - 2021):</strong><ul><li><strong>Sealed classes (stable):</strong> <code>sealed class Shape permits Circle, Square {}</code></li><li><strong>Pattern matching for switch (preview)</strong></li><li><strong>Strong encapsulation of JDK internals:</strong> no more <code>--add-opens</code> hacks</li><li><strong>Context-Specific Deserialization Filters</strong></li></ul><br><strong>Java 18 (2022):</strong><ul><li><strong>UTF-8 by default:</strong> standard charset</li><li><strong>Simple web server:</strong> <code>jwebserver</code> command</li><li><strong>Code snippets in Javadoc:</strong> <code>@snippet</code> tag</li></ul><br><strong>Java 19 (2022):</strong><ul><li><strong>Virtual threads (preview):</strong> lightweight threads for massive concurrency</li><li><strong>Structured concurrency (incubator)</strong></li><li><strong>Pattern matching for switch (3rd preview)</strong></li></ul><br><strong>Java 20 (2023):</strong><ul><li><strong>Scoped values (incubator):</strong> alternative to thread locals</li><li><strong>Record patterns (2nd preview)</strong></li></ul><br><strong>Java 21 (LTS - 2023):</strong><ul><li><strong>Virtual threads (stable):</strong> <code>Thread.ofVirtual()</code> — millions of lightweight threads</li><li><strong>Sequenced collections:</strong> <code>SequencedCollection</code> interface with <code>addFirst()</code>, <code>addLast()</code></li><li><strong>Pattern matching for switch (stable):</strong> <code>switch(obj) { case String s -> ...; }</code></li><li><strong>Record patterns (stable)</strong></li><li><strong>String templates (preview):</strong> <code>STR.\"Hello \\{name}\"</code></li></ul><br><strong>Java 22 (2024):</strong><ul><li><strong>Unnamed variables:</strong> <code>_ (underscore)</code> for unused variables</li><li><strong>String templates (2nd preview)</strong></li><li><strong>Statements before super() (preview):</strong> flexible constructor bodies</li><li><strong>Foreign Function & Memory API (2nd preview)</strong></li></ul><br><strong>Java 23 (2024):</strong><ul><li><strong>Markdown in Javadoc:</strong> write docs in Markdown</li><li><strong>Primitive types in patterns (preview)</strong></li><li><strong>Module import declarations (preview)</strong></li><li><strong>Flexible constructor bodies (2nd preview)</strong></li></ul><br><strong>Java 24-25 (Expected 2025-2026):</strong><ul><li>Continued evolution of Project Loom (virtual threads)</li><li>Project Amber features (pattern matching enhancements)</li><li>Project Panama (foreign functions)</li><li>Project Valhalla (value types - long-term)</li></ul><br><strong>Most critical for interviews:</strong><ul><li><strong>Java 8:</strong> Lambdas, Streams, Optional, Date/Time API (foundational)</li><li><strong>Java 11:</strong> String methods, HttpClient, var in lambdas (LTS)</li><li><strong>Java 17:</strong> Sealed classes, Records, Pattern matching for instanceof (LTS)</li><li><strong>Java 21:</strong> Virtual threads, Pattern matching for switch (latest LTS)</li></ul><br><strong>LTS versions to know:</strong> Java 8, 11, 17, 21 (next LTS: Java 25 in 2025)"},

  {q:"How does String.intern() vs String.valueOf() vs toString() differ?",d:"medium",
   a:"<strong>String.intern():</strong> returns the canonical representation from the <strong>String pool</strong>. If the pool already contains an equal string, returns that; otherwise, adds this string to the pool and returns it.<br><pre>String s1 = new String(\"hello\").intern();\nString s2 = \"hello\";\nSystem.out.println(s1 == s2); // true — both point to pooled \"hello\"</pre>Use case: deduplication when you have many repeated strings (e.g., parsing a large file with repeated tokens). Don't overuse — the pool is in native memory (PermGen in Java 7, heap in Java 8+).<br><br><strong>String.valueOf(obj):</strong> static method that converts an object to a String. If obj is null, returns <code>\"null\"</code> (does NOT throw NPE).<br><pre>String s = String.valueOf(null); // \"null\"\nString s = null.toString();      // NullPointerException!</pre>Internally: <code>obj == null ? \"null\" : obj.toString()</code>. Safe to use with nullable objects.<br><br><strong>toString():</strong> instance method on Object. Returns a string representation of the object. Default implementation: <code>ClassName@hashCode</code>. Most classes override it for meaningful output.<br><pre>new User(\"Alice\").toString(); // \"User(name=Alice)\" if overridden</pre><br><strong>Rule:</strong> use <code>String.valueOf()</code> when the object might be null; use <code>.toString()</code> when you know it's non-null."},

  {q:"What is WeakHashMap and does WeakHashSet exist in Java?",d:"medium",
   a:"<strong>WeakHashMap</strong> is a Map implementation where keys are <code>WeakReference</code>s — when a key has no strong references outside the map, it becomes eligible for GC and the entry is automatically removed.<br><br><strong>Use case:</strong> memory-sensitive caches where entries should disappear when keys are no longer used elsewhere in the application.<br><pre>WeakHashMap&lt;User, Session&gt; sessions = new WeakHashMap&lt;&gt;();\nUser user = new User(\"Alice\");\nsessions.put(user, session);\n\nuser = null;  // no strong reference to user anymore\nSystem.gc();  // suggest GC (not guaranteed immediately)\n// Eventually the user-&gt;session entry will be removed automatically</pre><br><strong>WeakHashSet does NOT exist</strong> in the standard Java library. To create a weak set, use:<br><pre>// Workaround: WeakHashMap with dummy values\nSet&lt;User&gt; weakSet = Collections.newSetFromMap(new WeakHashMap&lt;&gt;());\n\nweakSet.add(user);\nuser = null;  // entry will be collected</pre><br><strong>Important caveats:</strong><ul><li>WeakHashMap only weakens <em>keys</em>, not values — if a value holds a strong reference back to its key, the entry will never be collected (memory leak)</li><li>Iteration order is unpredictable as entries may be removed during iteration</li><li>Not thread-safe — wrap with <code>Collections.synchronizedMap()</code> if needed</li><li>GC timing is non-deterministic — don't rely on immediate cleanup</li></ul>"},

  {q:"What are List.of(), Set.of(), and Map.of() (Java 9+) and how do they differ from traditional collections?",d:"easy",
   a:"Java 9 introduced <strong>convenience factory methods</strong> for creating <strong>truly immutable</strong> collections — more efficient and safer than <code>Collections.unmodifiable*()</code> wrapper views.<br><br><strong>List.of():</strong><br><pre>List&lt;String&gt; list = List.of(\"a\", \"b\", \"c\");\nlist.add(\"d\");        // UnsupportedOperationException\nlist.set(0, \"x\");     // UnsupportedOperationException\n\n// Empty list\nList&lt;String&gt; empty = List.of();\n\n// Copy from existing collection\nList&lt;String&gt; copy = List.copyOf(existingList);</pre><br><strong>Set.of():</strong><br><pre>Set&lt;String&gt; set = Set.of(\"x\", \"y\", \"z\");\n\n// Duplicates throw IllegalArgumentException at creation time\nSet.of(\"a\", \"a\");  // throws!\n\n// Iteration order is randomized (security feature)\nSet.of(\"a\", \"b\", \"c\");  // may iterate as b, a, c</pre><br><strong>Map.of() and Map.ofEntries():</strong><br><pre>// Up to 10 key-value pairs\nMap&lt;String, Integer&gt; map = Map.of(\n  \"Alice\", 30,\n  \"Bob\", 25,\n  \"Charlie\", 35\n);\n\n// More than 10 entries - use ofEntries()\nimport static java.util.Map.entry;\n\nMap&lt;String, Integer&gt; big = Map.ofEntries(\n  entry(\"Alice\", 30),\n  entry(\"Bob\", 25),\n  entry(\"Charlie\", 35),\n  entry(\"Diana\", 28)\n  // ... up to any number of entries\n);</pre><br><strong>Key differences:</strong><br><table><tr><td></td><td><strong>List.of() / Set.of() / Map.of()</strong></td><td><strong>Traditional</strong></td></tr><tr><td>Mutability</td><td>❌ Truly immutable</td><td>✅ Mutable</td></tr><tr><td>Null values</td><td>❌ Throws NPE</td><td>✅ Allows nulls (except TreeSet, TreeMap)</td></tr><tr><td>Memory</td><td>Optimized, compact</td><td>Standard overhead</td></tr></table><br><strong>When to use:</strong><ul><li><strong>List.of() / Set.of() / Map.of():</strong> constants, configuration, test data, return values that shouldn't be modified</li><li><strong>Traditional collections:</strong> when you need mutability, nulls, or specific behavior (sorted, linked, concurrent)</li></ul>"}
);
SEC_JAVA.qs.push(
  {q:"What is constructor overloading and how does it work?",d:"easy",
   a:"<strong>Constructor overloading</strong> means defining multiple constructors in the same class with different parameter lists. The compiler chooses which constructor to call based on the arguments provided.<br><br><strong>Example:</strong><br><pre>public class Employee {\n  private String name;\n  private int age;\n  private String department;\n  \n  // No-arg constructor\n  public Employee() {\n    this(\"Unknown\", 0, \"General\");  // delegates to 3-param constructor\n  }\n  \n  // 2-param constructor\n  public Employee(String name, int age) {\n    this(name, age, \"General\");  // delegates to 3-param constructor\n  }\n  \n  // 3-param constructor (primary)\n  public Employee(String name, int age, String department) {\n    this.name = name;\n    this.age = age;\n    this.department = department;\n  }\n}</pre><br><strong>Rules:</strong><ul><li>Constructors must have different parameter types, number, or order</li><li>Use <code>this(...)</code> to call another constructor in the same class (must be first statement)</li><li>Return type is not part of signature — constructors have no return type</li><li>Common pattern: chain simpler constructors to the most complete one</li></ul>"},

  {q:"What is the difference between throw and throws?",d:"easy",
   a:"<code>throw</code> and <code>throws</code> are completely different keywords with different purposes:<br><br><strong>throw:</strong> used <em>inside</em> a method to actually throw an exception — an action.<br><pre>public void withdraw(double amount) {\n  if (amount > balance) {\n    throw new InsufficientFundsException(\"Not enough money\");  // action\n  }\n  balance -= amount;\n}</pre><br><strong>throws:</strong> used in method <em>signature</em> to declare that this method <em>might</em> throw an exception — a warning to callers.<br><pre>public void readFile(String path) throws IOException {  // declaration\n  FileReader reader = new FileReader(path);  // may throw IOException\n  // ...\n}</pre><br><strong>Key differences:</strong><br><table><tr><td></td><td><strong>throw</strong></td><td><strong>throws</strong></td></tr><tr><td>Location</td><td>Inside method body</td><td>Method signature</td></tr><tr><td>Purpose</td><td>Actually throw an exception</td><td>Declare possible exceptions</td></tr><tr><td>Usage</td><td><code>throw new Exception()</code></td><td><code>method() throws Exception</code></td></tr><tr><td>Count</td><td>Throws one exception instance</td><td>Can declare multiple exception types</td></tr></table><br><strong>Together:</strong><br><pre>public void process() throws IOException, SQLException {  // declares\n  if (invalidData) {\n    throw new IOException(\"Invalid format\");  // throws\n  }\n  // ...\n}</pre>"},

  {q:"What is the difference between Collection and Collections?",d:"easy",
   a:"<code>Collection</code> and <code>Collections</code> are completely different:<br><br><strong>Collection</strong> — an <strong>interface</strong> in <code>java.util</code>. The root interface of the collections framework hierarchy. Represents a group of objects.<br><br><strong>Collections</strong> — a <strong>utility class</strong> in <code>java.util</code>. Provides static helper methods for working with collections.<br><br><strong>Collection interface:</strong><br><pre>// Collection is the parent interface\npublic interface Collection&lt;E&gt; extends Iterable&lt;E&gt; {\n  boolean add(E e);\n  boolean remove(Object o);\n  int size();\n  boolean isEmpty();\n  Iterator&lt;E&gt; iterator();\n  // ...\n}\n\n// Implemented by List, Set, Queue\nCollection&lt;String&gt; list = new ArrayList&lt;&gt;();\nCollection&lt;String&gt; set = new HashSet&lt;&gt;();\nlist.add(\"item\");</pre><br><strong>Collections utility class:</strong><br><pre>// Collections provides static utility methods\npublic class Collections {\n  public static &lt;T&gt; void sort(List&lt;T&gt; list) { ... }\n  public static &lt;T&gt; int binarySearch(List&lt;T&gt; list, T key) { ... }\n  public static void reverse(List&lt;?&gt; list) { ... }\n  public static &lt;T&gt; List&lt;T&gt; synchronizedList(List&lt;T&gt; list) { ... }\n  // ...\n}\n\n// Usage\nList&lt;Integer&gt; nums = new ArrayList&lt;&gt;(List.of(3, 1, 2));\nCollections.sort(nums);  // [1, 2, 3]\nCollections.reverse(nums);  // [3, 2, 1]</pre><br><strong>Quick comparison:</strong><br><table><tr><td></td><td><strong>Collection</strong></td><td><strong>Collections</strong></td></tr><tr><td>Type</td><td>Interface</td><td>Utility class</td></tr><tr><td>Purpose</td><td>Define contract for collection types</td><td>Provide helper methods</td></tr><tr><td>Can instantiate?</td><td>❌ No (it's an interface)</td><td>❌ No (all methods are static)</td></tr><tr><td>Examples</td><td>List, Set, Queue extend it</td><td>sort(), shuffle(), reverse()</td></tr></table>"},

  {q:"What is the difference between Set and List?",d:"easy",
   a:"<code>Set</code> and <code>List</code> are both <code>Collection</code> interfaces but with fundamentally different behaviors:<br><br><strong>Key differences:</strong><br><table><tr><td></td><td><strong>List</strong></td><td><strong>Set</strong></td></tr><tr><td>Duplicates</td><td>✅ Allows duplicates</td><td>❌ No duplicates</td></tr><tr><td>Order</td><td>✅ Maintains insertion order</td><td>❌ No guaranteed order (except LinkedHashSet, TreeSet)</td></tr><tr><td>Null elements</td><td>✅ Allows multiple nulls</td><td>⚠️ At most one null (except TreeSet)</td></tr><tr><td>Index access</td><td>✅ get(index), set(index)</td><td>❌ No index-based access</td></tr><tr><td>Implementations</td><td>ArrayList, LinkedList, Vector</td><td>HashSet, LinkedHashSet, TreeSet</td></tr></table><br><strong>List example:</strong><br><pre>List&lt;String&gt; list = new ArrayList&lt;&gt;();\nlist.add(\"apple\");\nlist.add(\"banana\");\nlist.add(\"apple\");  // duplicate allowed ✅\n\nSystem.out.println(list);  // [apple, banana, apple]\nSystem.out.println(list.get(1));  // \"banana\" — index access ✅</pre><br><strong>Set example:</strong><br><pre>Set&lt;String&gt; set = new HashSet&lt;&gt;();\nset.add(\"apple\");\nset.add(\"banana\");\nset.add(\"apple\");  // ignored — duplicate rejected ❌\n\nSystem.out.println(set);  // [banana, apple] — order not guaranteed\n// No get(index) method — must iterate</pre><br><strong>When to use each:</strong><br><ul><li><strong>Use List when:</strong> Order matters, duplicates allowed, need index access. Example: order history, task queue.</li><li><strong>Use Set when:</strong> Uniqueness required, fast membership check (O(1) for HashSet). Example: unique user IDs, tags.</li></ul>"},

  {q:"Why is ArrayList.remove(0) slow and what's the alternative?",d:"medium",
   a:"<code>ArrayList.remove(0)</code> removes the first element but requires <strong>shifting all remaining elements</strong> one position to the left — O(n) time complexity.<br><br><strong>Why it's slow:</strong><br><pre>ArrayList: [10, 20, 30, 40, 50]\n           Remove index 0 (value 10)\n           \nStep 1: Delete 10\nStep 2: Shift 20 from index 1 → index 0\nStep 3: Shift 30 from index 2 → index 1\nStep 4: Shift 40 from index 3 → index 2\nStep 5: Shift 50 from index 4 → index 3\n\nResult: [20, 30, 40, 50]\n// Every element after index 0 was copied — expensive for large lists!</pre><br><strong>Performance comparison:</strong><ul><li><code>remove(0)</code> — O(n) — shifts all elements</li><li><code>remove(lastIndex)</code> — O(1) — no shifting needed</li><li><code>remove(middleIndex)</code> — O(n/2) — shifts half the elements</li></ul><br><strong>Alternative: Use ArrayDeque for frequent head removal</strong><br><pre>ArrayDeque&lt;Integer&gt; deque = new ArrayDeque&lt;&gt;();\ndeque.add(10); deque.add(20); deque.add(30);\n\ndeque.removeFirst();  // O(1) — just update head pointer\n// No element shifting required!</pre><br><strong>When to use each:</strong><ul><li><strong>ArrayList:</strong> random access (get/set), iterate, remove from end</li><li><strong>LinkedList:</strong> remove/add at head or tail</li><li><strong>ArrayDeque:</strong> best of both — efficient add/remove at both ends (preferred over LinkedList)</li></ul>"},

  {q:"How do you optimize ArrayList capacity and prevent wasted space?",d:"easy",
   a:"<code>ArrayList</code> internally uses an array that grows dynamically. When full, it creates a new array (typically 1.5× size) and copies all elements — expensive. You can optimize capacity management:<br><br><strong>1. Pre-size with initial capacity:</strong><br><pre>// If you know you'll add ~1000 elements\nArrayList&lt;String&gt; list = new ArrayList&lt;&gt;(1000);  // avoids multiple resizes\n\n// Default capacity is 10 — if you add 11th element, it resizes to 15, then 22, etc.</pre><br><strong>2. Trim unused space with trimToSize():</strong><br><pre>ArrayList&lt;Integer&gt; list = new ArrayList&lt;&gt;(1000);  // capacity = 1000\n// ... add 500 elements\nlist.size();      // 500 elements\n\nlist.trimToSize();  // shrinks internal array to exactly 500\n// Frees memory from unused 500 slots</pre><br><strong>3. Use ensureCapacity() to pre-grow:</strong><br><pre>ArrayList&lt;String&gt; list = new ArrayList&lt;&gt;();  // capacity = 10\nlist.ensureCapacity(10000);  // grow once to 10000 before loop\n\nfor (int i = 0; i &lt; 10000; i++) {\n  list.add(\"item\" + i);  // no resizing during loop!\n}</pre><br><strong>When to optimize:</strong><ul><li><strong>Pre-size</strong>: when you know the approximate size upfront (loading from DB, file)</li><li><strong>trimToSize()</strong>: after bulk operations when the list won't grow again (save memory)</li><li><strong>ensureCapacity()</strong>: before adding many elements in a loop (avoid repeated resizing)</li></ul><br><strong>Caveat:</strong> <code>trimToSize()</code> creates a new array and copies — only use when memory savings outweigh the copy cost."},

  {q:"Why is remove() faster in LinkedList than ArrayList for head/middle removal?",d:"medium",
   a:"<strong>ArrayList</strong> stores elements in a contiguous array. Removing an element (except the last) requires <strong>shifting</strong> all subsequent elements.<br><br><strong>LinkedList</strong> stores elements as nodes with pointers. Removing a node just <strong>updates pointers</strong> — no element copying.<br><br><strong>ArrayList remove(index):</strong><br><pre>// ArrayList: [A, B, C, D, E]\n// Remove index 1 (B)\n\nStep 1: Find element at index 1 — O(1)\nStep 2: Shift C from index 2 → index 1\nStep 3: Shift D from index 3 → index 2\nStep 4: Shift E from index 4 → index 3\nStep 5: Decrement size\n\nTime: O(n) — depends on how many elements need shifting</pre><br><strong>LinkedList remove(index):</strong><br><pre>// LinkedList: A ↔ B ↔ C ↔ D ↔ E\n// Remove B\n\nStep 1: Traverse to index 1 — O(n/2) on average\nStep 2: Update A.next = C\nStep 3: Update C.prev = A\nStep 4: Discard B node\n\nTime: O(n) for traversal, but O(1) for actual removal</pre><br><strong>Performance comparison:</strong><br><table><tr><td><strong>Operation</strong></td><td><strong>ArrayList</strong></td><td><strong>LinkedList</strong></td></tr><tr><td>get(index)</td><td>O(1) ✅</td><td>O(n) ❌</td></tr><tr><td>remove(0)</td><td>O(n) — shifts all</td><td>O(1) ✅</td></tr><tr><td>remove(middle)</td><td>O(n) — shifts half</td><td>O(n) — traverse half, then O(1) removal</td></tr><tr><td>remove(last)</td><td>O(1) ✅</td><td>O(1) ✅</td></tr><tr><td>add(index)</td><td>O(n)</td><td>O(n)</td></tr></table><br><strong>Key insight:</strong> LinkedList is faster for frequent head/tail removal (<code>removeFirst()</code>, <code>removeLast()</code>) because no traversal needed. But for random index removal, both are O(n) — ArrayList due to shifting, LinkedList due to traversal.<br><br><strong>Best choice:</strong><ul><li><strong>ArrayList:</strong> random access, iterate, infrequent modifications</li><li><strong>LinkedList:</strong> frequent head/tail add/remove (queue, deque operations)</li><li><strong>ArrayDeque:</strong> usually better than LinkedList — efficient head/tail operations with array backing</li></ul>"},

  {q:"Why does System.out.println('b' + 'i' + 't') print a number instead of 'bit'?",d:"easy",
   a:"In Java, <code>char</code> is a <strong>numeric type</strong> stored as 16-bit Unicode values. When you use the <code>+</code> operator on chars, Java performs <strong>numeric addition</strong>, not string concatenation.<br><br><strong>Example:</strong><br><pre>System.out.println('b' + 'i' + 't');\n// Output: 319  (not \"bit\"!)\n\n// Why?\n'b' = 98  (Unicode/ASCII value)\n'i' = 105\n't' = 116\n\nLeft-to-right evaluation:\n'b' + 'i' = 98 + 105 = 203\n203 + 't' = 203 + 116 = 319</pre><br><strong>How to get \"bit\" as a string:</strong><br><pre>// Method 1: Convert first char to String\nSystem.out.println(\"\" + 'b' + 'i' + 't');  // \"bit\" ✅\n// Empty string forces string concatenation\n\n// Method 2: Explicit String construction\nSystem.out.println(new String(new char[]{'b', 'i', 't'}));  // \"bit\"\n\n// Method 3: Use String.valueOf()\nSystem.out.println(String.valueOf('b') + 'i' + 't');  // \"bit\"</pre><br><strong>Key rule:</strong> <code>char + char</code> → numeric addition. To concatenate, ensure at least one operand is a <code>String</code>."},

  {q:"What is the execution order of static blocks, instance blocks, and constructors?",d:"medium",
   a:"Java executes initialization code in a specific order:<br><br><strong>Execution order:</strong><ol><li><strong>Static blocks</strong> — run once when class is loaded (before any instance creation)</li><li><strong>Instance blocks</strong> — run before constructor, every time an instance is created</li><li><strong>Constructor</strong> — runs after instance blocks</li></ol><br><strong>Complete example with output:</strong><br><pre>class Demo {\n  static int staticVar;\n  int instanceVar;\n  \n  // 1. Static block 1\n  static {\n    System.out.println(\"Static block 1, staticVar = \" + staticVar);  // 0\n  }\n  \n  // 2. Static block 2\n  static {\n    staticVar = 10;\n    System.out.println(\"Static block 2, staticVar = \" + staticVar);  // 10\n  }\n  \n  // 3. Instance block 1\n  {\n    System.out.println(\"Instance block 1, instanceVar = \" + instanceVar);  // 0\n  }\n  \n  // 4. Instance block 2\n  {\n    instanceVar = 5;\n    System.out.println(\"Instance block 2, instanceVar = \" + instanceVar);  // 5\n  }\n  \n  // 5. Constructor\n  Demo() {\n    System.out.println(\"Constructor, instanceVar = \" + instanceVar);  // 5\n  }\n  \n  public static void main(String[] args) {\n    System.out.println(\"=== Creating first instance ===\");\n    Demo d1 = new Demo();\n    \n    System.out.println(\"\\n=== Creating second instance ===\");\n    Demo d2 = new Demo();\n  }\n}</pre><br><strong>Output:</strong><br><pre>Static block 1, staticVar = 0\nStatic block 2, staticVar = 10\n=== Creating first instance ===\nInstance block 1, instanceVar = 0\nInstance block 2, instanceVar = 5\nConstructor, instanceVar = 5\n\n=== Creating second instance ===\nInstance block 1, instanceVar = 0\nInstance block 2, instanceVar = 5\nConstructor, instanceVar = 5</pre><br><strong>Key observations:</strong><ul><li>Static blocks run <strong>only once</strong> when class is first loaded</li><li>Instance blocks run <strong>every time</strong> an object is created</li><li>Multiple static/instance blocks execute in <strong>top-to-bottom</strong> order</li><li>Instance blocks run <strong>before</strong> the constructor</li><li>Use instance blocks for common initialization across multiple constructors</li></ul>"},

  {q:"What are the ways of making an object eligible for garbage collection?",d:"easy",
   a:"An object becomes eligible for garbage collection when it has <strong>no reachable references</strong> from any live thread or static context. Here are the common ways to make objects GC-eligible:<br><br><strong>1. Nullifying the reference:</strong><br><pre>Employee emp = new Employee(\"Alice\");\nemp = null;  // object now eligible for GC</pre><br><strong>2. Reassigning the reference:</strong><br><pre>Employee emp = new Employee(\"Alice\");\nemp = new Employee(\"Bob\");  // \"Alice\" object now eligible</pre><br><strong>3. Object created inside a method (goes out of scope):</strong><br><pre>void process() {\n  Employee emp = new Employee(\"Alice\");\n  // use emp\n}  // emp goes out of scope, object eligible for GC</pre><br><strong>4. Island of Isolation (circular references with no external reference):</strong><br><pre>class Node {\n  Node next;\n}\n\nNode n1 = new Node();\nNode n2 = new Node();\nn1.next = n2;\nn2.next = n1;  // circular reference\n\nn1 = null;\nn2 = null;  // Both objects eligible despite circular ref\n// Modern GC handles this</pre><br><strong>5. Anonymous objects:</strong><br><pre>new Employee(\"Alice\").work();  // no reference stored, eligible immediately after use</pre><br><strong>6. Explicit System.gc() request (not guaranteed):</strong><br><pre>emp = null;\nSystem.gc();  // suggests GC, but JVM may ignore</pre><br><strong>Important notes:</strong><ul><li>Setting reference to <code>null</code> doesn't immediately GC the object — just makes it eligible</li><li><code>System.gc()</code> is a suggestion, not a command — JVM decides when to run GC</li><li>Modern JVMs use sophisticated algorithms (G1GC, ZGC) that track reachability automatically</li><li>Static references keep objects alive for the entire program lifetime</li><li>WeakReference, SoftReference allow GC even with references present</li></ul>"},

  {q:"What is the Java Reflection API and when should you use it?",d:"medium",
   a:"<strong>Reflection</strong> allows you to inspect and manipulate classes, methods, fields, and constructors at <strong>runtime</strong> — even private members. It's the foundation for frameworks, serialization, and dependency injection.<br><br><strong>Core Reflection APIs:</strong><br><pre>// Get Class object\nClass&lt;?&gt; clazz = String.class;  // compile-time known\nClass&lt;?&gt; clazz = obj.getClass();  // from instance\nClass&lt;?&gt; clazz = Class.forName(\"java.lang.String\");  // from name\n\n// Inspect methods\nMethod[] methods = clazz.getDeclaredMethods();  // all methods (including private)\nMethod m = clazz.getMethod(\"substring\", int.class);  // public method by name\nm.invoke(obj, 5);  // call obj.substring(5)\n\n// Inspect fields\nField[] fields = clazz.getDeclaredFields();\nField f = clazz.getDeclaredField(\"value\");\nf.setAccessible(true);  // bypass private access\nObject value = f.get(obj);  // read private field\n\n// Create instances\nConstructor&lt;?&gt; ctor = clazz.getConstructor(String.class);\nObject instance = ctor.newInstance(\"hello\");</pre><br><strong>Practical example — Generic toString():</strong><br><pre>public static String toString(Object obj) throws Exception {\n  Class&lt;?&gt; clazz = obj.getClass();\n  StringBuilder sb = new StringBuilder(clazz.getSimpleName() + \"{\");\n  \n  for (Field field : clazz.getDeclaredFields()) {\n    field.setAccessible(true);  // access private fields\n    sb.append(field.getName()).append(\"=\")\n      .append(field.get(obj)).append(\", \");\n  }\n  \n  return sb.toString().replaceAll(\", $\", \"}\");\n}\n\n// Usage\nPerson p = new Person(\"Alice\", 30);\nSystem.out.println(toString(p));  // Person{name=Alice, age=30}</pre><br><strong>Common use cases:</strong><ul><li><strong>Frameworks:</strong> Spring DI, Hibernate ORM — inject dependencies, map DB to objects</li><li><strong>Serialization:</strong> Jackson, Gson — convert objects to JSON without explicit getters</li><li><strong>Testing:</strong> Mockito — create mock objects, access private methods for testing</li><li><strong>Annotations:</strong> JUnit <code>@Test</code>, Spring <code>@Autowired</code> — discover annotated elements</li><li><strong>Dynamic proxies:</strong> AOP, RPC — intercept method calls</li></ul><br><strong>Downsides:</strong><ul><li><strong>Performance:</strong> 10-100× slower than direct access</li><li><strong>Security:</strong> can break encapsulation, access private members</li><li><strong>Compile-time safety:</strong> typos in method/field names only caught at runtime</li><li><strong>Compatibility:</strong> refactoring (rename field) can break reflection code</li></ul><br><strong>Best practice:</strong> Use reflection in <em>frameworks</em> and <em>tools</em>, not application code. For application logic, prefer interfaces and polymorphism."},

  {q:"What are Deque, BlockingQueue, and PriorityQueue and when to use each?",d:"medium",
   a:"Java provides specialized queue interfaces for different use cases:<br><br><strong>1. Deque (Double-Ended Queue) — add/remove from both ends</strong><br><pre>Deque&lt;String&gt; deque = new ArrayDeque&lt;&gt;();\n\n// Operations at head (front)\ndeque.addFirst(\"A\");    // Add to front\ndeque.removeFirst();    // Remove from front\ndeque.peekFirst();      // View front without removing\n\n// Operations at tail (back)\ndeque.addLast(\"B\");     // Add to back\ndeque.removeLast();     // Remove from back\ndeque.peekLast();       // View back without removing\n\n// Can be used as Stack (LIFO)\ndeque.push(\"C\");  // addFirst\ndeque.pop();      // removeFirst\n\n// Can be used as Queue (FIFO)\ndeque.offer(\"D\");  // addLast\ndeque.poll();      // removeFirst</pre><strong>Implementations:</strong> <code>ArrayDeque</code> (most common), <code>LinkedList</code><br><strong>Use case:</strong> when you need stack, queue, or both ends access (e.g., work-stealing queues, undo/redo)<br><br><strong>2. BlockingQueue — thread-safe queue with blocking operations</strong><br><pre>BlockingQueue&lt;Task&gt; queue = new LinkedBlockingQueue&lt;&gt;(10);  // capacity 10\n\n// Producer thread\nqueue.put(task);  // blocks if queue is full ⏳\n\n// Consumer thread\nTask task = queue.take();  // blocks if queue is empty ⏳\n\n// Non-blocking alternatives\nqueue.offer(task, 1, TimeUnit.SECONDS);  // wait up to 1 sec, then give up\nTask task = queue.poll(1, TimeUnit.SECONDS);  // wait up to 1 sec, return null if empty</pre><strong>Implementations:</strong> <code>ArrayBlockingQueue</code> (bounded), <code>LinkedBlockingQueue</code> (optionally bounded), <code>PriorityBlockingQueue</code>, <code>DelayQueue</code><br><strong>Use case:</strong> producer-consumer pattern, thread pools, concurrent task processing<br><br><strong>3. PriorityQueue — heap-based queue, elements ordered by priority</strong><br><pre>// Natural ordering (min-heap)\nPriorityQueue&lt;Integer&gt; pq = new PriorityQueue&lt;&gt;();\npq.offer(5);\npq.offer(1);\npq.offer(3);\n\nSystem.out.println(pq.poll());  // 1 (smallest first)\nSystem.out.println(pq.poll());  // 3\nSystem.out.println(pq.poll());  // 5\n\n// Custom comparator (max-heap)\nPriorityQueue&lt;Integer&gt; maxHeap = new PriorityQueue&lt;&gt;(Collections.reverseOrder());\nmaxHeap.offer(5);\nmaxHeap.offer(1);\nmaxHeap.offer(3);\nSystem.out.println(maxHeap.poll());  // 5 (largest first)\n\n// Complex objects\nPriorityQueue&lt;Task&gt; taskQueue = new PriorityQueue&lt;&gt;(\n  Comparator.comparingInt(Task::getPriority)\n);</pre><strong>Characteristics:</strong><ul><li>Heap-based — O(log n) insertion/removal</li><li>NOT thread-safe (use <code>PriorityBlockingQueue</code> for concurrency)</li><li>Does NOT guarantee order when iterating — only <code>poll()</code> returns min/max</li></ul><strong>Use case:</strong> task scheduling (process highest priority first), Dijkstra's algorithm, event processing<br><br><strong>Quick comparison:</strong><br><table><tr><td><strong>Interface</strong></td><td><strong>Key Feature</strong></td><td><strong>Use Case</strong></td></tr><tr><td>Deque</td><td>Both ends access</td><td>Stack, queue, work stealing</td></tr><tr><td>BlockingQueue</td><td>Thread-safe blocking</td><td>Producer-consumer, thread pools</td></tr><tr><td>PriorityQueue</td><td>Priority ordering</td><td>Task scheduling, shortest-job-first</td></tr></table>"},

  {q:"What are coupling and cohesion in software design?",d:"medium",
   a:"<strong>Coupling</strong> and <strong>cohesion</strong> are fundamental principles for designing maintainable systems. Good design aims for <strong>low coupling</strong> and <strong>high cohesion</strong>.<br><br><strong>Coupling — how much classes depend on each other</strong><br><br><strong>Low coupling (good):</strong> classes are independent, changes in one don't force changes in others<br><pre>// Low coupling — depends on interface, not implementation\npublic class OrderService {\n  private final PaymentProcessor processor;  // interface\n  \n  public OrderService(PaymentProcessor processor) {\n    this.processor = processor;  // injected dependency\n  }\n  \n  public void processOrder(Order order) {\n    processor.process(order.getTotal());  // doesn't care about implementation\n  }\n}\n\n// Easy to swap implementations\nOrderService service = new OrderService(new StripePaymentProcessor());\nOrderService service = new OrderService(new PayPalPaymentProcessor());</pre><br><strong>High coupling (bad):</strong> classes tightly depend on each other's internals<br><pre>// High coupling — directly creates dependency\npublic class OrderService {\n  public void processOrder(Order order) {\n    StripePaymentProcessor processor = new StripePaymentProcessor();  // tight coupling!\n    processor.setApiKey(\"sk_test_123\");  // knows internal details\n    processor.chargeCard(order.getCardNumber(), order.getTotal());\n  }\n}\n// Hard to test, hard to change payment provider</pre><br><strong>Cohesion — how focused a class is on a single responsibility</strong><br><br><strong>High cohesion (good):</strong> class has a single, well-defined purpose<br><pre>// High cohesion — only handles user authentication\npublic class AuthenticationService {\n  public boolean authenticate(String username, String password) { ... }\n  public boolean validateToken(String token) { ... }\n  public void logout(String sessionId) { ... }\n}\n// All methods relate to authentication</pre><br><strong>Low cohesion (bad):</strong> class does too many unrelated things<br><pre>// Low cohesion — mixed responsibilities\npublic class UserManager {\n  public void authenticateUser(String username, String password) { ... }\n  public void sendEmail(String to, String subject) { ... }\n  public void generateReport(Date from, Date to) { ... }\n  public void processPayment(double amount) { ... }\n}\n// Violates Single Responsibility Principle</pre><br><strong>Summary:</strong><br><table><tr><td></td><td><strong>Coupling</strong></td><td><strong>Cohesion</strong></td></tr><tr><td>Definition</td><td>Dependency between classes</td><td>Focus within a class</td></tr><tr><td>Goal</td><td>Low (loose) coupling</td><td>High cohesion</td></tr><tr><td>Good sign</td><td>Classes independent</td><td>Single responsibility</td></tr><tr><td>Bad sign</td><td>Change ripples across classes</td><td>Class does too many things</td></tr><tr><td>Achieve with</td><td>Interfaces, DI, abstraction</td><td>SRP, clear purpose</td></tr></table><br><strong>Benefits of low coupling + high cohesion:</strong><ul><li>Easier to understand (each class has clear purpose)</li><li>Easier to test (mock dependencies, test in isolation)</li><li>Easier to maintain (change one class without breaking others)</li><li>Easier to reuse (focused classes can be used in different contexts)</li></ul>"}
);

SEC_JAVA.qs.push(
  {q:"Output: Integer comparison within cache range (==)",d:"easy",
   a:"<strong>Code:</strong><br><pre>Integer a = 100;\nInteger b = 100;\nSystem.out.println(a == b);</pre><br><strong>Output: true</strong><br><br><strong>Explanation:</strong> Java caches <code>Integer</code> objects in the range <strong>-128 to 127</strong> for performance. When you create <code>Integer a = 100</code> and <code>Integer b = 100</code>, both reference the <em>same cached object</em> from the Integer pool.<br><pre>Integer a = 100;  // references cached Integer(100)\nInteger b = 100;  // references same cached Integer(100)\nSystem.out.println(a == b);  // true — same object reference</pre><br><strong>Contrast with 200:</strong><br><pre>Integer a = 200;  // new Integer(200) on heap\nInteger b = 200;  // different new Integer(200) on heap\nSystem.out.println(a == b);  // false — different objects\nSystem.out.println(a.equals(b));  // true — same value</pre><br><strong>Rule:</strong> Always use <code>.equals()</code> for Integer comparison, never <code>==</code> (unless checking for null or intentionally checking reference equality)."},

  {q:"Output: Integer comparison outside cache range (==)",d:"easy",
   a:"<strong>Code:</strong><br><pre>Integer a = 200;\nInteger b = 200;\nSystem.out.println(a == b);</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> 200 is <strong>outside the Integer cache range</strong> (-128 to 127), so each assignment creates a <em>new Integer object</em> on the heap.<br><pre>Integer a = 200;  // new Integer object\nInteger b = 200;  // different new Integer object\nSystem.out.println(a == b);  // false — different references\nSystem.out.println(a.equals(b));  // true — same value</pre><br><strong>Key insight:</strong> The cache is an optimization for commonly-used small integers. Outside the cache, autoboxing always creates new objects."},

  {q:"Output: Auto-unboxing null Integer to int",d:"easy",
   a:"<strong>Code:</strong><br><pre>Integer a = null;\nint b = a;</pre><br><strong>Output: NullPointerException</strong><br><br><strong>Explanation:</strong> Auto-unboxing converts <code>Integer</code> to <code>int</code> by calling <code>a.intValue()</code>. When <code>a</code> is <code>null</code>, this throws <code>NullPointerException</code>.<br><pre>Integer a = null;\nint b = a;  // tries to call a.intValue() → NPE!</pre><br><strong>Safe alternative:</strong><br><pre>Integer a = null;\nint b = (a != null) ? a : 0;  // explicit null check\n// or\nint b = Objects.requireNonNullElse(a, 0);  // Java 9+</pre><br><strong>Rule:</strong> Always check for null before auto-unboxing wrapper types to primitives."},

  {q:"Output: String concatenation with mixed int addition",d:"easy",
   a:"<strong>Code:</strong><br><pre>System.out.println(1 + 2 + \"3\" + 4 + 5);</pre><br><strong>Output: 3345</strong><br><br><strong>Explanation:</strong> Java evaluates expressions <strong>left-to-right</strong>. Once a <code>String</code> appears, all subsequent <code>+</code> operations become string concatenation.<br><pre>1 + 2       // 3 (int addition)\n3 + \"3\"     // \"33\" (string concatenation starts)\n\"33\" + 4    // \"334\" (string concatenation)\n\"334\" + 5   // \"3345\" (string concatenation)</pre><br><strong>Contrast:</strong><br><pre>System.out.println(\"3\" + 1 + 2);  // \"312\" (all concatenation)\nSystem.out.println(\"3\" + (1 + 2));  // \"33\" (parentheses force addition first)</pre>"},

  {q:"Output: String concatenation starting with String literal",d:"easy",
   a:"<strong>Code:</strong><br><pre>System.out.println(\"1\" + 2 + 3 + 4);</pre><br><strong>Output: 1234</strong><br><br><strong>Explanation:</strong> The first operand is a <code>String</code>, so <em>all</em> subsequent <code>+</code> operations are string concatenations, not additions.<br><pre>\"1\" + 2    // \"12\" (concatenation)\n\"12\" + 3   // \"123\" (concatenation)\n\"123\" + 4  // \"1234\" (concatenation)</pre><br><strong>Key rule:</strong> If <em>any</em> operand in a <code>+</code> operation is a String, the result is string concatenation, not numeric addition."},

  {q:"Output: Post-increment and pre-increment in same expression",d:"medium",
   a:"<strong>Code:</strong><br><pre>int i = 1;\ni = i++ + ++i;\nSystem.out.println(i);</pre><br><strong>Output: 4</strong><br><br><strong>Explanation:</strong> This involves both post-increment (<code>i++</code>) and pre-increment (<code>++i</code>).<br><br><strong>Step-by-step evaluation:</strong><br><pre>int i = 1;\ni = i++ + ++i;\n\nStep 1: Evaluate i++\n  - Returns current value: 1\n  - Then increments i: i becomes 2\n\nStep 2: Evaluate ++i\n  - Increments i first: i becomes 3\n  - Returns new value: 3\n\nStep 3: Add results\n  - 1 + 3 = 4\n\nStep 4: Assign to i\n  - i = 4</pre><br><strong>Warning:</strong> Mixing pre/post increment in the same expression is <strong>bad practice</strong> — hard to read and error-prone. Avoid in production code."},

  {q:"Output: Complex expression with multiple increments",d:"hard",
   a:"<strong>Code:</strong><br><pre>int i = 5;\nint j = i++ + ++i + i++;\nSystem.out.println(j);</pre><br><strong>Output: 19</strong><br><br><strong>Explanation:</strong> Complex mix of post and pre increments evaluated left-to-right.<br><br><strong>Step-by-step:</strong><br><pre>int i = 5;\nint j = i++ + ++i + i++;\n\n// Evaluate i++\n  Returns: 5, then i becomes 6\n\n// Evaluate ++i  \n  i becomes 7, returns: 7\n\n// Evaluate i++\n  Returns: 7, then i becomes 8\n\n// Add results\n  5 + 7 + 7 = 19\n\n// Final: j = 19, i = 8</pre><br><strong>Mnemonic:</strong> <code>i++</code> = \"use then increment\", <code>++i</code> = \"increment then use\".<br><br><strong>Best practice:</strong> Never write code like this. Split into separate lines for clarity."},

  {q:"Output: String literal comparison with ==",d:"easy",
   a:"<strong>Code:</strong><br><pre>String s1 = \"hello\";\nString s2 = \"hello\";\nSystem.out.println(s1 == s2);</pre><br><strong>Output: true</strong><br><br><strong>Explanation:</strong> String literals are stored in the <strong>String Pool</strong>. When you write <code>\"hello\"</code> twice, the JVM reuses the same String object from the pool.<br><pre>String s1 = \"hello\";  // creates or retrieves from pool\nString s2 = \"hello\";  // retrieves same object from pool\nSystem.out.println(s1 == s2);  // true — same reference</pre><br><strong>Why this works:</strong> String interning optimizes memory by ensuring only one instance of each literal string exists."},

  {q:"Output: String object comparison with new keyword",d:"easy",
   a:"<strong>Code:</strong><br><pre>String s1 = new String(\"hello\");\nString s2 = new String(\"hello\");\nSystem.out.println(s1 == s2);</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> <code>new String()</code> <strong>always creates a new object</strong> on the heap, bypassing the String Pool.<br><pre>String s1 = new String(\"hello\");  // new heap object\nString s2 = new String(\"hello\");  // different new heap object\nSystem.out.println(s1 == s2);      // false — different references\nSystem.out.println(s1.equals(s2)); // true — same content</pre><br><strong>Avoid this:</strong> Using <code>new String(\"literal\")</code> is wasteful — use string literals directly."},

  {q:"Output: Compile-time String constant folding",d:"medium",
   a:"<strong>Code:</strong><br><pre>String s1 = \"hello\" + \"world\";\nString s2 = \"helloworld\";\nSystem.out.println(s1 == s2);</pre><br><strong>Output: true</strong><br><br><strong>Explanation:</strong> The compiler performs <strong>compile-time constant folding</strong> — it evaluates <code>\"hello\" + \"world\"</code> at compile time and replaces it with <code>\"helloworld\"</code>.<br><pre>// At compile time:\nString s1 = \"hello\" + \"world\";  // becomes \"helloworld\" literal\nString s2 = \"helloworld\";\n\n// Both reference the same pooled string\nSystem.out.println(s1 == s2);  // true</pre><br><strong>Important:</strong> This only works with compile-time constants (literals). Variables break this optimization."},

  {q:"Output: Runtime String concatenation with variables",d:"medium",
   a:"<strong>Code:</strong><br><pre>String s1 = \"ja\";\nString s2 = \"va\";\nString s3 = s1 + s2;\nString s4 = \"java\";\nSystem.out.println(s3 == s4);</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> When concatenating <em>variables</em> (not literals), Java cannot optimize at compile time. <code>s1 + s2</code> creates a <strong>new String object</strong> at runtime.<br><pre>String s1 = \"ja\";\nString s2 = \"va\";\nString s3 = s1 + s2;     // runtime concatenation → new object on heap\nString s4 = \"java\";      // pooled literal\n\nSystem.out.println(s3 == s4);       // false — different objects\nSystem.out.println(s3.equals(s4));  // true — same value</pre><br><strong>To pool s3:</strong><br><pre>String s3 = (s1 + s2).intern();  // add to pool\nSystem.out.println(s3 == s4);     // true</pre>"},

  {q:"Output: String.equals() with null (Yoda condition)",d:"easy",
   a:"<strong>Code:</strong><br><pre>String s = null;\nSystem.out.println(\"abc\".equals(s));</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> Calling <code>\"abc\".equals(null)</code> is <strong>null-safe</strong> — it returns <code>false</code> instead of throwing <code>NullPointerException</code>.<br><pre>String s = null;\nSystem.out.println(\"abc\".equals(s));  // false — safe null check</pre><br><strong>Best practice:</strong> Always put the <em>known non-null string first</em> (\"Yoda condition\") to avoid NPE:<br><pre>// Bad\nif (s.equals(\"abc\"))  // NPE if s is null\n\n// Good\nif (\"abc\".equals(s))  // safe — returns false if s is null</pre>"},

  {q:"Output: Calling equals() on null reference",d:"easy",
   a:"<strong>Code:</strong><br><pre>String s = null;\nSystem.out.println(s.equals(\"abc\"));</pre><br><strong>Output: NullPointerException</strong><br><br><strong>Explanation:</strong> Calling a method on <code>null</code> throws <code>NullPointerException</code>.<br><pre>String s = null;\nSystem.out.println(s.equals(\"abc\"));  // NPE — calling method on null</pre><br><strong>Fix:</strong> Use Yoda condition (literal first) or explicit null check:<br><pre>// Safe approaches\nSystem.out.println(\"abc\".equals(s));       // false\nSystem.out.println(Objects.equals(s, \"abc\"));  // false (Java 7+)</pre>"},

  {q:"Output: Finally block execution with return in try",d:"easy",
   a:"<strong>Code:</strong><br><pre>try {\n  return;\n} finally {\n  System.out.println(\"finally\");\n}</pre><br><strong>Output: finally</strong><br><br><strong>Explanation:</strong> The <code>finally</code> block <strong>always executes</strong> before the method returns, even when there's a <code>return</code> statement in the <code>try</code> block.<br><pre>try {\n  return;  // prepares to return\n} finally {\n  System.out.println(\"finally\");  // executes first\n}\n// Output: \"finally\"\n// Then method returns</pre><br><strong>Exception:</strong> <code>finally</code> does NOT execute if <code>System.exit()</code> is called."},

  {q:"Output: Finally return overrides try return",d:"medium",
   a:"<strong>Code:</strong><br><pre>static int test() {\n  try {\n    return 1;\n  } finally {\n    return 2;\n  }\n}\nSystem.out.println(test());</pre><br><strong>Output: 2</strong><br><br><strong>Explanation:</strong> When <code>finally</code> contains a <code>return</code> statement, it <strong>overrides</strong> the return value from the <code>try</code> block.<br><pre>static int test() {\n  try {\n    return 1;  // discarded\n  } finally {\n    return 2;  // this wins\n  }\n}\nSystem.out.println(test());  // 2</pre><br><strong>Warning:</strong> Returning from <code>finally</code> is <strong>bad practice</strong> and confusing. Avoid it. Some static analysis tools flag this as an error."},

  {q:"Output: Finally modifying variable after return evaluation",d:"medium",
   a:"<strong>Code:</strong><br><pre>static int test() {\n  int x = 1;\n  try {\n    return x;\n  } finally {\n    x = 2;\n  }\n}\nSystem.out.println(test());</pre><br><strong>Output: 1</strong><br><br><strong>Explanation:</strong> The return value is <strong>evaluated and copied</strong> before the <code>finally</code> block executes. Changing <code>x</code> in <code>finally</code> doesn't affect the already-prepared return value.<br><pre>static int test() {\n  int x = 1;\n  try {\n    return x;  // evaluates to 1, stores in temp variable\n  } finally {\n    x = 2;  // changes local x, but return value already set\n  }\n}\nSystem.out.println(test());  // 1</pre><br><strong>Key insight:</strong> Return value is captured before <code>finally</code> runs."},

  {q:"Output: Static block execution order before main()",d:"easy",
   a:"<strong>Code:</strong><br><pre>class Test {\n  static {\n    System.out.println(\"A\");\n  }\n  public static void main(String[] args) {\n    System.out.println(\"B\");\n  }\n}</pre><br><strong>Output: A B</strong><br><br><strong>Explanation:</strong> <strong>Static blocks execute when the class is loaded</strong>, before any static methods (including <code>main</code>) are called.<br><pre>class Test {\n  static {\n    System.out.println(\"A\");  // runs at class load time\n  }\n  public static void main(String[] args) {\n    System.out.println(\"B\");  // runs when main is called\n  }\n}\n// Output: A B</pre><br><strong>Execution order:</strong><ol><li>Class loaded by JVM</li><li>Static blocks run (top to bottom)</li><li>main() method executes</li></ol>"},

  {q:"Output: Method overload resolution: primitive vs wrapper",d:"medium",
   a:"<strong>Code:</strong><br><pre>void print(int a) {\n  System.out.println(\"int\");\n}\nvoid print(Integer a) {\n  System.out.println(\"Integer\");\n}\nprint(10);</pre><br><strong>Output: int</strong><br><br><strong>Explanation:</strong> When both primitive and wrapper versions exist, Java prefers the <strong>exact match (primitive)</strong> over auto-boxing.<br><pre>void print(int a) { System.out.println(\"int\"); }\nvoid print(Integer a) { System.out.println(\"Integer\"); }\n\nprint(10);  // \"int\" — exact match, no boxing needed</pre><br><strong>Overload resolution order:</strong><ol><li>Exact type match</li><li>Widening primitive conversion (int → long)</li><li>Auto-boxing (int → Integer)</li><li>Varargs</li></ol>"},

  {q:"Output: Method overload resolution: exact match vs varargs",d:"medium",
   a:"<strong>Code:</strong><br><pre>void print(int... a) {\n  System.out.println(\"varargs\");\n}\nvoid print(int a) {\n  System.out.println(\"int\");\n}\nprint(10);</pre><br><strong>Output: int</strong><br><br><strong>Explanation:</strong> Java prefers <strong>exact match over varargs</strong>. Varargs is the last resort in overload resolution.<br><pre>void print(int... a) { System.out.println(\"varargs\"); }\nvoid print(int a) { System.out.println(\"int\"); }\n\nprint(10);  // \"int\" — exact match preferred\nprint(10, 20);  // \"varargs\" — only option for multiple args</pre><br><strong>Overload priority:</strong><ol><li>Exact match</li><li>Widening</li><li>Auto-boxing</li><li>Varargs (lowest priority)</li></ol>"},

  {q:"Output: List.remove() with Integer: index vs value",d:"medium",
   a:"<strong>Code:</strong><br><pre>List&lt;Integer&gt; list = new ArrayList&lt;&gt;();\nlist.add(1);\nlist.add(2);\nlist.remove(1);\nSystem.out.println(list);</pre><br><strong>Output: [1]</strong><br><br><strong>Explanation:</strong> <code>list.remove(1)</code> removes by <strong>index</strong>, not by value. Index 1 contains the value 2.<br><pre>List&lt;Integer&gt; list = new ArrayList&lt;&gt;();\nlist.add(1);  // [1]\nlist.add(2);  // [1, 2]\nlist.remove(1);  // removes index 1 → removes value 2\nSystem.out.println(list);  // [1]</pre><br><strong>To remove by value:</strong><br><pre>list.remove(Integer.valueOf(1));  // removes value 1\n// or\nlist.remove((Integer) 1);  // explicit cast to Integer</pre><br><strong>Rule:</strong> <code>remove(int index)</code> vs <code>remove(Object o)</code> — for <code>List&lt;Integer&gt;</code>, primitive int is treated as index."},

  {q:"Output: ConcurrentModificationException during iteration",d:"medium",
   a:"<strong>Code:</strong><br><pre>List&lt;Integer&gt; list = new ArrayList&lt;&gt;(List.of(1,2,3));\nfor(Integer i : list) {\n  list.remove(i);\n}</pre><br><strong>Output: ConcurrentModificationException</strong><br><br><strong>Explanation:</strong> You cannot modify a collection while iterating over it with a <strong>for-each loop</strong>. The iterator detects structural modification and throws <code>ConcurrentModificationException</code>.<br><pre>List&lt;Integer&gt; list = new ArrayList&lt;&gt;(List.of(1,2,3));\nfor(Integer i : list) {\n  list.remove(i);  // modifies collection during iteration → exception\n}</pre><br><strong>Safe alternatives:</strong><br><pre>// Use explicit iterator\nIterator&lt;Integer&gt; it = list.iterator();\nwhile (it.hasNext()) {\n  it.next();\n  it.remove();  // safe removal\n}\n\n// Use removeIf (Java 8+)\nlist.removeIf(i -&gt; i % 2 == 0);  // removes evens</pre><br><strong>Fail-fast behavior</strong> protects against concurrent modification bugs."},

  {q:"Output: Thread.run() vs Thread.start()",d:"medium",
   a:"<strong>Code:</strong><br><pre>Thread t = new Thread(() -&gt; System.out.println(\"Thread\"));\nt.run();\nSystem.out.println(\"Main\");</pre><br><strong>Output: Thread Main</strong><br><br><strong>Explanation:</strong> Calling <code>t.run()</code> executes the thread's <code>run()</code> method in the <strong>current thread</strong> (main thread), not in a new thread. To start a new thread, use <code>t.start()</code>.<br><pre>Thread t = new Thread(() -&gt; System.out.println(\"Thread\"));\nt.run();  // runs in main thread, not a new thread\nSystem.out.println(\"Main\");\n// Output: Thread Main (sequential, same thread)</pre><br><strong>Correct usage:</strong><br><pre>Thread t = new Thread(() -&gt; System.out.println(\"Thread\"));\nt.start();  // starts new thread\nSystem.out.println(\"Main\");\n// Output: Main Thread or Thread Main (order unpredictable)</pre><br><strong>Rule:</strong> <code>start()</code> = new thread. <code>run()</code> = same thread."},

  {q:"Output: NaN equality comparison (IEEE 754)",d:"medium",
   a:"<strong>Code:</strong><br><pre>System.out.println(Double.NaN == Double.NaN);</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> <code>NaN</code> (Not-a-Number) is <strong>not equal to itself</strong> by IEEE 754 specification. This is the only value in Java where <code>x == x</code> is false.<br><pre>System.out.println(Double.NaN == Double.NaN);  // false\nSystem.out.println(Double.NaN != Double.NaN);  // true!</pre><br><strong>To check for NaN:</strong><br><pre>// Wrong\nif (x == Double.NaN)  // always false\n\n// Right\nif (Double.isNaN(x))  // correct way\nif (x != x)           // works but obscure</pre><br><strong>Why:</strong> NaN represents undefined results (e.g., 0.0/0.0, sqrt(-1)). Two undefined values shouldn't be considered equal."},

  {q:"Output: StringBuilder.toString() creates new object",d:"easy",
   a:"<strong>Code:</strong><br><pre>StringBuilder sb = new StringBuilder(\"abc\");\nString s = sb.toString();\nSystem.out.println(s == sb.toString());</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> <code>toString()</code> creates a <strong>new String object</strong> each time it's called.<br><pre>StringBuilder sb = new StringBuilder(\"abc\");\nString s = sb.toString();  // creates new String object\nSystem.out.println(s == sb.toString());  // false — different objects\nSystem.out.println(s.equals(sb.toString()));  // true — same content</pre><br><strong>Always use <code>.equals()</code></strong> for string comparison, never <code>==</code>."},

  {q:"Output: List.contains() with cross-type comparison",d:"medium",
   a:"<strong>Code:</strong><br><pre>System.out.println(Arrays.asList(1,2,3).contains(1L));</pre><br><strong>Output: false</strong><br><br><strong>Explanation:</strong> The list contains <code>Integer</code> objects (1, 2, 3), but you're checking for a <code>Long</code> (1L). <code>Integer</code> and <code>Long</code> are different types — <code>.equals()</code> returns false for cross-type comparison.<br><pre>Arrays.asList(1,2,3).contains(1L);  // false\n// List contains: [Integer(1), Integer(2), Integer(3)]\n// Searching for: Long(1)\n// Integer(1).equals(Long(1)) → false</pre><br><strong>Fix:</strong><br><pre>Arrays.asList(1,2,3).contains(1);  // true — same type</pre>"},

  {q:"Output: Math.round() with negative numbers",d:"medium",
   a:"<strong>Code:</strong><br><pre>System.out.println(Math.round(-2.5));</pre><br><strong>Output: -2</strong><br><br><strong>Explanation:</strong> <code>Math.round()</code> rounds toward <strong>positive infinity</strong>, which means it rounds <em>up</em> for both positive and negative numbers.<br><pre>Math.round(2.5)   // 3  (rounds up)\nMath.round(-2.5)  // -2 (rounds up toward zero)\nMath.round(-2.6)  // -3 (rounds down away from zero)</pre><br><strong>Rule:</strong> Adds 0.5 and truncates toward zero.<br><pre>Math.round(-2.5) = (int)(-2.5 + 0.5) = (int)(-2.0) = -2</pre><br><strong>For floor/ceiling:</strong><br><pre>Math.floor(-2.5)  // -3.0\nMath.ceil(-2.5)   // -2.0</pre>"},

  {q:"Output: Array toString() default behavior",d:"easy",
   a:"<strong>Code:</strong><br><pre>int[] arr = {1,2,3};\nSystem.out.println(arr);</pre><br><strong>Output: [I@hashcode</strong> (e.g., <code>[I@15db9742</code>)<br><br><strong>Explanation:</strong> Arrays don't override <code>toString()</code>, so they print the default <code>Object.toString()</code> format: <code>ClassName@HashCode</code>.<br><pre>int[] arr = {1,2,3};\nSystem.out.println(arr);  // [I@15db9742\n// [I = array of int\n// @ = separator\n// 15db9742 = hashcode in hex</pre><br><strong>To print array contents:</strong><br><pre>System.out.println(Arrays.toString(arr));  // [1, 2, 3]\n\n// Multi-dimensional arrays\nint[][] arr2d = {{1,2}, {3,4}};\nSystem.out.println(Arrays.deepToString(arr2d));  // [[1, 2], [3, 4]]</pre>"},

  {q:"Output: Stream reuse throws IllegalStateException",d:"medium",
   a:"<strong>Code:</strong><br><pre>Stream&lt;Integer&gt; s = Stream.of(1,2,3);\ns.forEach(System.out::println);\ns.forEach(System.out::println);</pre><br><strong>Output: IllegalStateException (stream has already been operated upon or closed)</strong><br><br><strong>Explanation:</strong> Streams can only be consumed <strong>once</strong>. After a terminal operation (<code>forEach</code>), the stream is closed and cannot be reused.<br><pre>Stream&lt;Integer&gt; s = Stream.of(1,2,3);\ns.forEach(System.out::println);  // 1 2 3 — consumes stream\ns.forEach(System.out::println);  // IllegalStateException — stream already used</pre><br><strong>To reuse:</strong><br><pre>// Option 1: Store the source and create new streams\nList&lt;Integer&gt; list = List.of(1,2,3);\nlist.stream().forEach(System.out::println);  // first use\nlist.stream().forEach(System.out::println);  // second use\n\n// Option 2: Use Supplier to create streams on demand\nSupplier&lt;Stream&lt;Integer&gt;&gt; streamSupplier = () -&gt; Stream.of(1,2,3);\nstreamSupplier.get().forEach(System.out::println);\nstreamSupplier.get().forEach(System.out::println);</pre>"}
);
