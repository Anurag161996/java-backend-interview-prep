const SEC_MICRO = {
  id:"microservices",title:"Microservices & Spring Cloud",icon:"☁",ib:"rgba(56,189,248,.12)",ic:"#38bdf8",
  grp:"arch",bar:"#38bdf8",tglow:"rgba(56,189,248,.07)",
  desc:"Service discovery, Resilience4j, distributed tracing, Config Server, API Gateway, sidecar, and migration.",
  tags:["Service discovery","Resilience4j","Tracing","Config Server","API Gateway","Strangler fig"],
  qs:[
    {q:"What are the key principles of microservices architecture?",d:"medium", id:1,
     a:"<strong>Single Responsibility:</strong> each service owns exactly one bounded context — a cohesive domain area. It should be small enough that the team can understand the full codebase and redeploy within a sprint.<br><br><strong>Decentralised Data Ownership:</strong> each service owns its own database. No shared schemas. No service directly queries another service's DB. This is the most important and most violated rule — shared DB creates tight coupling and prevents independent deployment.<br><br><strong>Independent Deployability:</strong> any service can be deployed without coordinating with other teams. Requires backward-compatible API changes and consumer-driven contract testing.<br><br><strong>Failure Isolation:</strong> one service's failure should not cascade. Use circuit breakers, bulkheads, timeouts, and fallbacks.<br><br><strong>API-First Communication:</strong> services interact via well-defined APIs (REST, gRPC, events). The API is a contract.<br><br><strong>Conway's Law:</strong> system architecture mirrors team communication structure. Microservices work best when team boundaries align with service boundaries (Inverse Conway Manoeuvre)."},

    {q:"What is service discovery and how does it differ from DNS-based routing?",d:"medium", id:2,
     a:"<strong>Problem:</strong> in a dynamic environment (containers, auto-scaling), service IPs change constantly. Hardcoding IPs doesn't work.<br><br><strong>Service registry solution:</strong> services register themselves on startup with their host, port, metadata, and health status. Other services query the registry to find available instances.<br><br><strong>Client-side discovery</strong> (Spring Cloud / Eureka):<ol><li>Service registers with Eureka: <code>@EnableEurekaClient</code></li><li>Caller queries Eureka: <code>@LoadBalanced RestTemplate</code> or <code>WebClient</code> resolves service names</li><li>Spring Cloud LoadBalancer picks an instance (Round Robin by default)</li></ol><br><strong>Server-side discovery</strong> (AWS ALB, Kubernetes):<ol><li>Client calls a stable DNS name or VIP</li><li>Load balancer queries the registry and routes transparently</li><li>Client is unaware of multiple instances</li></ol><br><strong>Kubernetes DNS-based:</strong> each Service gets a stable DNS entry (<code>my-service.namespace.svc.cluster.local</code>) regardless of pod IP changes. Preferred in k8s environments over Eureka."},

    {q:"What is Resilience4j and what patterns does it provide?",d:"medium", id:3,
     a:"Resilience4j is the modern fault-tolerance library for Java (successor to Hystrix, which is no longer maintained). It is lightweight, modular, and integrates with Spring Boot via <code>spring-cloud-starter-circuitbreaker-resilience4j</code>.<br><br><strong>Circuit Breaker:</strong> fail-fast on repeated failures. Configurable failure rate threshold, slow call threshold.<br><br><strong>Retry:</strong><br><pre>@Retry(name = \"orderService\", fallbackMethod = \"retryFallback\")\npublic Order fetchOrder(String id) { ... }</pre>Config: <code>maxAttempts=3</code>, <code>waitDuration=500ms</code>, <code>enableExponentialBackoff=true</code>, <code>exponentialBackoffMultiplier=2</code>, <code>randomizedWaitFactor=0.5</code> (jitter).<br><br><strong>Rate Limiter:</strong> limit outgoing call rate to protect downstream services from overload.<br><br><strong>Bulkhead:</strong> isolate a thread pool or semaphore per dependency — prevents a slow downstream from starving all other operations.<br><br><strong>Time Limiter:</strong> timeout on async calls — <code>CompletableFuture</code> is cancelled if it exceeds the duration.<br><br>All patterns expose <strong>Micrometer metrics</strong> automatically — monitor via Prometheus/Grafana/Datadog."},

    {q:"How do you implement distributed tracing across microservices?",d:"hard", id:4,
     a:"Distributed tracing follows a single request across multiple service hops, giving you a flame graph of where time was spent.<br><br><strong>Core concepts:</strong><ul><li><strong>Trace ID:</strong> a single ID shared across the entire request journey (generated at the entry point)</li><li><strong>Span ID:</strong> a unique ID for each leg of the journey (each service hop or DB call)</li><li><strong>Parent Span ID:</strong> links spans into a tree structure</li></ul><br><strong>Propagation:</strong> W3C TraceContext standard (<code>traceparent: 00-traceId-spanId-flags</code> header) — propagated across HTTP, Kafka, gRPC.<br><br><strong>Spring Boot 3 + Micrometer Tracing:</strong><br><pre>spring:\n  application:\n    name: order-service\n  tracing:\n    sampling:\n      probability: 1.0  # 100% in dev, 10% in prod\n\nmanagement:\n  tracing:\n    enabled: true</pre>Micrometer auto-instruments <code>RestTemplate</code>, <code>WebClient</code>, Feign, <code>@Async</code>, Kafka, and JDBC.<br><br><strong>Export backends:</strong> Zipkin, Jaeger, Datadog APM, Grafana Tempo. Inject trace ID into log MDC for correlated log search."},

    {q:"What is the strangler fig migration pattern?",d:"medium", id:5,
     a:"Named after the parasitic fig tree that slowly wraps around and eventually replaces its host tree — without killing it during the transition.<br><br><strong>Goal:</strong> incrementally migrate a monolith to microservices without a risky big-bang rewrite.<br><br><strong>Steps:</strong><ol><li>Put a proxy/API Gateway in front of the monolith</li><li>Identify a bounded context to extract (pick one that is well-isolated in the monolith)</li><li>Build the new microservice for that context</li><li>Route traffic for that context to the new service (via gateway routing rules)</li><li>Remove that code from the monolith</li><li>Repeat until the monolith is empty and can be decommissioned</li></ol><br><strong>Key principles:</strong><ul><li>Always have a deployable, working system at every step</li><li>No feature freeze — both systems evolve during migration</li><li>The monolith and microservices coexist, sometimes sharing a DB temporarily</li></ul><br><strong>Spring Cloud Gateway example:</strong> route <code>/api/payments/**</code> to new payment service; everything else still goes to monolith."},

    {q:"What is the sidecar pattern and what is a service mesh?",d:"hard", id:6,
     a:"<strong>Sidecar pattern:</strong> deploy an auxiliary container alongside each service instance (same pod in Kubernetes). The sidecar proxy handles infrastructure concerns so the main service can focus on business logic:<ul><li>mTLS between all services (encryption + mutual authentication)</li><li>Retries and circuit breaking</li><li>Distributed tracing (inject headers, report spans)</li><li>Load balancing and traffic shaping</li><li>Access control policies</li></ul>All of this without any code changes to the service.<br><br><strong>Service mesh:</strong> a fleet of sidecars (data plane) managed by a central control plane.<ul><li><strong>Istio:</strong> most full-featured. Envoy proxies as sidecars. Control plane: Istiod.</li><li><strong>Linkerd:</strong> simpler, lighter, Rust-based proxies.</li><li><strong>AWS App Mesh:</strong> managed service mesh on AWS.</li></ul><br><strong>Trade-offs:</strong> significant operational complexity, steep learning curve, overhead per request. Justified for large microservice estates (50+ services) with multiple teams. Overkill for small systems."}
  ]
};

SEC_MICRO.qs.push(
  {q:"How do you implement API versioning in a microservices gateway?",d:"medium", id:7,
     a:"When evolving microservices APIs, the gateway can handle versioning transparently so backend services don't all need to change simultaneously.<br><br><strong>Spring Cloud Gateway routing by version:</strong><br><pre>spring:\n  cloud:\n    gateway:\n      routes:\n        - id: orders-v2\n          uri: lb://order-service-v2\n          predicates:\n            - Path=/api/v2/orders/**\n        - id: orders-v1\n          uri: lb://order-service-v1\n          predicates:\n            - Path=/api/v1/orders/**</pre><br><strong>Header-based routing:</strong><br><pre>predicates:\n  - Header=X-Api-Version, v2</pre><br><strong>Strangler Fig via gateway:</strong> old service handles all routes; gradually cut over individual paths to the new service as they're ready. Gateway enables blue/green deployments (route 10% to v2, monitor errors, then shift 100%).<br><br><strong>Contract testing</strong> (Pact): consumer-driven contract tests ensure the new version doesn't break existing consumers before deploying."},

  {q:"What is the difference between synchronous and asynchronous inter-service communication?",d:"medium", id:8,
     a:"<strong>Synchronous (REST/gRPC):</strong> caller waits for the response before continuing.<ul><li>✅ Simple to implement and reason about</li><li>✅ Immediate error feedback</li><li>✅ Natural request/response semantics</li><li>❌ Temporal coupling — both services must be up simultaneously</li><li>❌ Cascading failures — slow downstream makes caller slow</li><li>❌ Tight availability coupling</li></ul><br><strong>Asynchronous (Kafka/SQS/RabbitMQ):</strong> caller publishes an event and moves on; consumer processes independently.<ul><li>✅ Decoupled availability — producer can run while consumer is down</li><li>✅ Natural backpressure — queue absorbs spikes</li><li>✅ Easy fan-out to multiple consumers</li><li>❌ Eventual consistency — caller can't know immediately if it succeeded</li><li>❌ More complex error handling (DLQ, retries)</li><li>❌ Harder to debug (distributed trace needed)</li></ul><br><strong>Rule of thumb:</strong> use sync for queries needing an immediate response; async for state-changing events where eventual consistency is acceptable."}
);

SEC_MICRO.qs.push(

  {q:"How do inter-service calls authenticate with each other in a microservices system?",d:"hard", id:9,
     a:"In a microservices system, services must trust each other — but you can't simply open all internal traffic without authentication.<br><br><strong>Option 1 — JWT propagation:</strong> forward the user's JWT from the original request through all downstream service calls. Each service validates the JWT independently.<br><pre>// Feign client interceptor — propagate token\n@Component\npublic class JwtPropagatingInterceptor implements RequestInterceptor {\n  public void apply(RequestTemplate template) {\n    String token = SecurityContextHolder.getContext()\n      .getAuthentication().getCredentials().toString();\n    template.header(\"Authorization\", \"Bearer \" + token);\n  }\n}</pre><br><strong>Option 2 — Service-to-service JWT (M2M):</strong> each service has its own client credentials (OAuth2 client_credentials flow). Services authenticate as themselves, not on behalf of a user. Use for background jobs and internal APIs.<br><br><strong>Option 3 — mTLS (mutual TLS):</strong> each service has a certificate. Both sides verify the other's certificate — strongest guarantee. Handled automatically by a service mesh (Istio/Linkerd).<br><br><strong>Option 4 — API keys:</strong> internal API key per calling service, rotated periodically. Simpler but coarser-grained than JWT.<br><br><strong>Best practice:</strong> use JWT propagation for user-context calls + mTLS (via service mesh) for network-level trust."},

  {q:"What is the bulkhead pattern and how do you implement it with Resilience4j?",d:"hard", id:10,
     a:"The bulkhead pattern isolates failures by limiting concurrent calls to each downstream dependency — preventing one slow service from monopolising all threads and starving others.<br><br><strong>Named after watertight compartments in a ship:</strong> if one compartment floods, others stay dry.<br><br><strong>Two bulkhead types in Resilience4j:</strong><br><br><strong>Semaphore bulkhead</strong> (best for virtual threads / reactive):<br><pre>@Bulkhead(name = \"paymentService\", fallbackMethod = \"bulkheadFallback\")\npublic Payment charge(Order order) { ... }\n\n// application.yml:\nresilience4j.bulkhead:\n  instances:\n    paymentService:\n      maxConcurrentCalls: 10      # max 10 parallel calls\n      maxWaitDuration: 500ms      # wait up to 500ms for a slot</pre><br><strong>Thread pool bulkhead</strong> (for isolated execution):<br><pre>resilience4j.thread-pool-bulkhead:\n  instances:\n    paymentService:\n      maxThreadPoolSize: 10\n      coreThreadPoolSize: 5\n      queueCapacity: 20</pre><br><strong>Result:</strong> if the payment service is slow, only 10 threads are ever blocked — inventory, shipping, and notification services all have their own threads and remain responsive."}
);

SEC_MICRO.qs.push(

  {q:"How do you implement health checks for Kubernetes probes in Spring Boot?",d:"medium", id:11,
     a:"Kubernetes uses three probe types to manage pod lifecycle — each mapped to a specific Spring Boot Actuator endpoint.<br><br><strong>Enable Spring Boot health probes:</strong><br><pre>management.health.probes.enabled=true\nmanagement.endpoint.health.show-details=always</pre><br><strong>Liveness probe</strong> — is the app alive? If it fails repeatedly, Kubernetes restarts the pod:<br><pre># k8s pod spec:\nlivenessProbe:\n  httpGet:\n    path: /actuator/health/liveness\n    port: 8080\n  initialDelaySeconds: 30\n  periodSeconds: 10\n  failureThreshold: 3</pre>Spring's liveness indicator fails if the application is in a broken internal state (e.g., deadlock). Use <code>LivenessStateHealthIndicator</code>.<br><br><strong>Readiness probe</strong> — is the app ready to serve traffic? If it fails, Kubernetes removes the pod from Service endpoints:<br><pre>readinessProbe:\n  httpGet:\n    path: /actuator/health/readiness\n    port: 8080\n  initialDelaySeconds: 10\n  periodSeconds: 5</pre>Fails when the app is starting up, draining, or when a dependency (DB, Kafka) is unavailable.<br><br><strong>Startup probe</strong> — gives slow-starting apps time before liveness kicks in:<br><pre>startupProbe:\n  httpGet:\n    path: /actuator/health/liveness\n    port: 8080\n  failureThreshold: 30  # 30 × 10s = 5 min to start</pre>"}
);

SEC_MICRO.qs.push(
  {q:"What is the BFF (Backend for Frontend) pattern and when do you use it?",d:"medium", id:12,
     a:"<strong>BFF (Backend for Frontend):</strong> a separate backend service tailored for each frontend client (web, mobile, IoT) — instead of forcing all clients to use the same generic API.<br><br><strong>Problem without BFF:</strong> mobile app needs a lightweight payload (only 5 fields), web app needs 20 fields, and smart TV needs a different format entirely. A single API either over-fetches or under-fetches for every client.<br><br><strong>With BFF:</strong><br><ul><li><strong>web-bff:</strong> aggregates data from 5 microservices, returns a rich response</li><li><strong>mobile-bff:</strong> returns minimal JSON, pre-resized images</li><li><strong>tv-bff:</strong> returns large text, voice-friendly responses</li></ul><br><pre>mobile-app → mobile-bff → [order-service, user-service, payment-service]\nweb-app    → web-bff    → [order-service, user-service, inventory-service]</pre><br><strong>Benefits:</strong><ul><li>Each BFF optimized for its client (payload size, format, caching strategy)</li><li>Backend microservices remain generic and reusable</li><li>Frontend teams own their BFF (autonomous evolution)</li></ul><br><strong>Pitfall:</strong> can lead to duplication if BFFs share logic — use shared libraries or a shared aggregation layer.<br><br><strong>Implementation:</strong> Spring Boot service per client type, or GraphQL (client specifies what it needs)."},

  {q:"How do you handle zero-downtime database schema migrations in microservices?",d:"hard", id:13,
     a:"<strong>Challenge:</strong> old code expects old schema, new code expects new schema. Deploy both simultaneously without breaking either.<br><br><strong>Backward-compatible migrations (expand-contract pattern):</strong><br><br><strong>Phase 1 — Expand (add new columns, keep old ones):</strong><br><pre>-- Migration V1: add new column, nullable\nALTER TABLE users ADD COLUMN full_name VARCHAR(255);</pre>Deploy new code that writes to BOTH <code>full_name</code> and old <code>first_name</code>/<code>last_name</code>. Old code still reads old columns.<br><br><strong>Phase 2 — Migrate data (backfill):</strong><br><pre>UPDATE users SET full_name = first_name || ' ' || last_name WHERE full_name IS NULL;</pre><br><strong>Phase 3 — Contract (remove old columns after all code updated):</strong><br>Once all services deployed with new code that reads <code>full_name</code>, drop old columns:<br><pre>-- Migration V2 (weeks later):\nALTER TABLE users DROP COLUMN first_name, DROP COLUMN last_name;</pre><br><strong>Non-nullable columns:</strong> add as nullable first, backfill, then add NOT NULL constraint later.<br><br><strong>Renaming columns:</strong> never rename directly — create new column, dual-write, migrate data, drop old.<br><br><strong>Flyway best practices:</strong> never edit old migrations — only add new ones. Use <code>baseline-on-migrate</code> for existing databases."},

  {q:"How do you implement distributed rate limiting across multiple instances?",d:"hard", id:14,
     a:"<strong>Problem:</strong> you have 10 instances of a service. You want 1000 requests/minute total across all instances — not 1000 per instance.<br><br><strong>Option 1 — Redis sliding window (shared state):</strong><br>Use a shared Redis instance to track request counts. All service instances increment the same counter.<br><pre>-- Lua script (atomic):\nlocal key = \"rate:user:\" .. KEYS[1]\nlocal limit = tonumber(ARGV[1])\nlocal window = tonumber(ARGV[2])\n\nlocal count = redis.call('INCR', key)\nif count == 1 then\n  redis.call('EXPIRE', key, window)\nend\n\nif count &gt; limit then\n  return 0 -- rate limited\nelse\n  return 1 -- allowed\nend</pre><br>Every instance checks/increments the same Redis key — true distributed rate limiting.<br><br><strong>Option 2 — Redis Sorted Set (sliding window, more accurate):</strong><br>Store timestamps of requests in a sorted set, count requests in the last N seconds.<br><pre>ZREMRANGEBYSCORE rate:user:123 0 (now - 60s)\nZCARD rate:user:123\nif count &lt; limit: ZADD rate:user:123 now now</pre><br><strong>Option 3 — Token Bucket with Redis:</strong> store <code>tokens</code> and <code>last_refill_time</code> in Redis, refill at a fixed rate.<br><br><strong>Spring Boot integration:</strong><br><pre>@Autowired RedisTemplate redisTemplate;\n\nif (checkRateLimit(userId)) {\n  // process request\n} else {\n  throw new RateLimitExceededException();\n}</pre><br><strong>Alternative — API Gateway:</strong> offload rate limiting to Kong, AWS API Gateway, or Spring Cloud Gateway with Redis-backed rate limiter."},

  {q:"What is service mesh vs API gateway — which handles what?",d:"medium", id:15,
     a:"Both manage traffic between services, but they operate at different layers and solve different problems.<br><br><strong>API Gateway (north-south traffic — external → internal):</strong><br><ul><li><strong>Entry point</strong> for all external traffic (mobile apps, web, third-party APIs)</li><li>Handles: authentication, rate limiting, request routing, response aggregation, API versioning, TLS termination</li><li>Examples: Kong, AWS API Gateway, Spring Cloud Gateway, Apigee</li><li>Positioned at the edge of your system</li></ul><br><strong>Service Mesh (east-west traffic — service → service):</strong><br><ul><li><strong>Sidecar proxy</strong> deployed alongside every service instance</li><li>Handles: mTLS between services, service discovery, load balancing, retries, circuit breaking, observability (distributed tracing), traffic splitting (canary)</li><li>Examples: Istio, Linkerd, Consul Connect</li><li>Operates within the cluster, invisible to application code</li></ul><br><strong>When to use both:</strong> API Gateway handles ingress (external requests), service mesh handles internal service-to-service communication. They complement each other.<br><br><strong>Example flow:</strong><br><pre>Mobile App → API Gateway (auth, rate limit) → Order Service (via Istio sidecar, mTLS) → Payment Service (via sidecar)</pre><br><strong>When NOT to use service mesh:</strong> simple systems with &lt;5 services — operational complexity outweighs benefits. Start with an API gateway and add service mesh when you need advanced traffic management."}
);